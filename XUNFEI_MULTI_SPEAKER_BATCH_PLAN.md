# 讯飞多人配音合并生成方案

## 一、目标

将当前“每个文本段单独生成”的方式改为：

```text
解析文档
→ 构造多人配音内容
→ 一次生成完整作品
→ 下载完整音频
→ 按内部停顿安全切割
→ 恢复为每题一个 MP3
```

最终交付格式保持不变，仍然是每个题目一个音频文件。

作品批次不再按照音色分组，只在超过讯飞网页的文本长度限制时拆分。当前讯飞配音编辑页显示单次最多约 10000 字，具体实现时需要为多人配音格式标记预留长度空间。批次内部的页面操作仍会把连续且音色、语速、语调、音量完全相同的行合并为一次选区，以减少重复设置次数；不连续的相同配置不会跨过其他音色强行合并。

系统同时保留原有的逐段生成方式，作为用户可选的兼容模式。音色配置页增加生成方式选择，默认使用“全部生成后切割”。

参考页面：[讯飞配音编辑页](https://peiyin.xunfei.cn/make)

## 二、现有代码基础

当前项目已经具备两个可复用部分：

- [wordtts/speakers.py:52](/Users/asklins/Documents/xiaozhu_workspace/edge-tts-webui-main/wordtts/speakers.py:52) 已经能够解析 W/M 和自定义角色。
- [xunfei/submission_tracker.py:72](/Users/asklins/Documents/xiaozhu_workspace/edge-tts-webui-main/xunfei/submission_tracker.py:72) 监听多人配音相关响应并捕获作品 ID；生成提交本身必须由可见页面按钮触发。

目前的问题是，实际生成流程仍然在 [wordtts/batch.py:326](/Users/asklins/Documents/xiaozhu_workspace/edge-tts-webui-main/wordtts/batch.py:326) 处理多人配音合并作品。因此当前的“批量”主要优化了合并作品的提交和下载流程，并没有改变最终按题目恢复音频的交付格式。

## 三、多人配音内容构造

每个逻辑片段保留以下信息：

```text
segment_id
item_id
segment_index
文本内容
角色
音色
语速
语调
音量
```

然后通过讯飞多人配音编辑器构造：

```text
片段 1：女声文本
停顿标记
片段 2：男声文本
停顿标记
片段 3：自定义角色文本
停顿标记
```

网页本身已经支持“多人配音”和可插入的停顿节点，因此实现固定模拟可见网页操作，不直接调用 `makeMultipleSpeakerWork` 或 `order_gen` 伪造生成提交。

页面操作顺序固定为：

```text
输入每个逻辑行
→ 精确选中一行或连续同配置的多行
→ 点击“多人配音”
→ 搜索并选择音色
→ 键盘设置语速/语调/音量并回读
→ 点击“使用”并校验 speaker 标记
→ 在每道题末尾插入页面停顿按钮
→ 点击“生成音频”
```

选区安全规则：

- 单行使用该段落的文本选择，不允许退化为编辑器全选；
- 多行只拖选当前连续配置组，并将 `window.getSelection()` 的文本与目标行回读比对；
- 拖选不精确时只拆成逐行操作，不在校验失败时继续套用音色；
- 点击“使用”后逐段回读 `data-speaker-id`/音色标签，确认目标行真的被页面标记。

实施时已通过登录后的讯飞网页样例确认：

- 多人配音请求的字段结构；
- 每段文本如何绑定音色；
- 每段是否支持独立语速、语调、音量；
- 生成后是否返回一个完整作品；
- 页面点击生成后的作品响应与现有下载流程是否兼容。

页面面板支持每个选区独立设置音色和三项参数。参数回读失败、音色标记回读失败或选区边界不明确时，当前作品直接失败并保留日志，不猜测页面状态，也不自动增加额外计费提交。

## 四、合并批次和长度限制

在“全部生成后切割”模式下，同一次任务内优先将所有逻辑片段按原始顺序放入同一个多人配音作品中。

只有出现以下情况时才拆成多个合并批次：

- 总文本超过网页显示的 10000 字限制；
- 多人配音编辑器或请求接口存在更小的实际限制；
- 某些特殊文本导致网页无法正常提交。

拆分必须发生在完整逻辑片段之间，不能把一个片段从中间截断。批次之间记录明确的片段边界，最终仍按原始顺序恢复。

## 五、停顿和音频切割策略

停顿只作为内部定位标记，不能直接保留到最终音频中。

### 1. 合并音频中的停顿

停顿可以使用 0.5 秒或 1 秒，具体时长根据实际波形测试确定。

停顿的作用是制造清晰的切割区，而不是让最终每段音频出现长时间空白。

### 2. 最终切割方式

对于：

```text
音频 A + 人工停顿 + 音频 B
```

切割时需要：

- 定位人工停顿形成的完整低能量区；
- 删除中间的大部分人工停顿；
- 音频 A 只保留很短的尾部保护静音；
- 音频 B 只保留很短的首部保护静音；
- 不在声音刚结束或刚开始的位置直接切；
- 不使用字符比例或平均时长推算边界；
- 不对所有片段执行激进的“去首尾静音”。

原则是：

> 最终不能出现明显的长空档，但宁可保留几十毫秒保护空间，也不能裁掉尾音、送气音或首辅音。

切割算法建议使用保守的双阈值静音检测，并在停顿标记附近寻找候选低能量区。只有当候选区同时满足长度、能量和左右保护距离要求时才允许切割。

如果某个边界没有足够安全的静音区，则该批次切割失败，不强行按比例切割。

### 3. 防止首尾空档过长

不能简单在停顿中点切割，因为这样会让前一段和后一段各自保留较长的空白。

推荐的处理方式是：

```text
前段：保留少量安全尾部静音
中间：删除大部分人工停顿
后段：保留少量安全首部静音
```

保护时长应做成可调参数，初始值通过实际音频样本校准。切割完成后不再执行全局静音裁剪，避免二次处理吞掉弱音。

## 六、最终文件恢复

切割后的片段根据以下字段恢复原始顺序：

```text
item_id
segment_index
```

同一道题包含多个角色时，按原始顺序重新拼接；不同题目保持独立文件。前端结果页、下载接口和 ZIP 文件结构原则上无需改变。

## 七、断点和异常处理

每个合并作品需要保存：

```text
批次 ID
包含的逻辑片段
合并文本指纹
worksId
下载状态
切割状态
临时合并音频路径
```

这样可以避免：

- 下载失败后重复提交；
- 程序中断后重复扣次数；
- 切割失败时无法重新分析原始合并音频。

如果讯飞已经返回 worksId，应优先重新下载，不能直接重新生成。切割失败时应保留临时合并音频用于诊断，并默认禁止自动回退为逐段生成，避免在用户不知情的情况下增加计费次数。

## 八、进度和用户界面

最终文件接口保持现有格式，但内部进度需要区分：

- 合并作品数量；
- 已提交的合并作品；
- 已下载的合并作品；
- 已切割的逻辑片段；
- 已导出的最终文件。

结果页仍然展示每题一个音频。生成日志可以额外显示类似：

```text
正在生成 1 个多人配音作品，完成后切割为 48 个音频片段
```

## 九、用户可选的生成模式

### 1. 模式定义

在音色配置页面增加生成方式选项：

```text
全部生成后切割（默认）
单条单条生成（原有方式）
```

配置字段建议统一为：

```text
generation_mode: "composite_cut" | "single_segment"
```

其中：

- `composite_cut`：使用讯飞多人配音，一次生成合并作品，再按停顿切割；
- `single_segment`：沿用当前逐逻辑片段生成、统一下载和按题目拼接的流程。

新配置和没有该字段的旧配置默认使用 `composite_cut`。用户可以在配置页主动切换为 `single_segment`。已有历史任务不重新生成，历史记录按照任务生成时保存的模式展示；旧历史记录没有模式字段时，按原有的 `single_segment` 解释。

### 2. 两种模式共用的部分

两种模式都共用以下流程：

- 文档解析；
- W/M 和自定义角色解析；
- 音色配置和参数规范化；
- 最终题目音频导出；
- ZIP 打包；
- 历史记录保存；
- 结果页文件列表和播放接口。

这样可以保证用户切换模式后，最终文件结构和使用方式不变。

### 3. 单条单条生成模式

`single_segment` 完全沿用现有逻辑：

```text
逻辑片段
→ 单条提交讯飞
→ 统一下载
→ 按题目顺序拼接
→ 导出最终 MP3
```

该模式不插入多人配音停顿，不进行合并音频切割，也不改变现有音频边界行为。它主要用于：

- 用户更看重每段独立生成的稳定性；
- 多人配音网页能力发生变化或暂时不可用；
- 合并音频切割校验失败后的人工重试；
- 对首尾音频边界要求特别严格的任务。

### 4. 合并后切割模式

`composite_cut` 使用本文前面设计的多人配音流程：

```text
逻辑片段
→ 构造多人配音作品
→ 一次提交并下载
→ 安全定位停顿
→ 删除大部分人工停顿
→ 恢复单段音频
```

该模式默认启用，但必须在切割数量、边界安全性和最终首尾空档校验通过后才算成功。

### 5. 配置、预设和断点规则

- 生成方式需要保存到当前配置和配置预设；
- 服务端必须重新规范化 `generation_mode`，不能只信任前端；
- 生成方式属于任务核心配置，必须写入进度、历史清单和 ZIP manifest；
- 任务开始后不允许在同一个断点任务中途切换模式；
- 如果用户切换模式后重试，应清理或隔离另一模式生成的临时产物；
- 断点恢复时必须要求模式、解析版本和音频算法版本一致。

## 十、兼容两种模式的进度与日志设计

### 1. 进度字段分层

为了兼容现有前端，现有 `stats` 事件中的以下字段继续保留原含义：

```text
total       最终题目总数
completed   已完成并可交付的题目数
failed      最终失败的题目数
processed   面向最终题目的综合处理进度
pending     尚未完成的最终题目数
phase       当前阶段
```

在此基础上增加可选的模式字段：

```json
{
  "generation_mode": "composite_cut",
  "work": {
    "kind": "composite_batch",
    "total": 1,
    "submitted": 1,
    "downloaded": 1,
    "sliced": 18,
    "exported": 10
  },
  "segments": {
    "total": 48,
    "downloaded": 48,
    "sliced": 18,
    "exported": 10
  }
}
```

旧模式则返回相同结构，但 `work.kind` 为 `single_segment`：

```json
{
  "generation_mode": "single_segment",
  "work": {
    "kind": "single_segment",
    "total": 48,
    "submitted": 20,
    "downloaded": 18,
    "sliced": 0,
    "exported": 10
  }
}
```

旧版前端只读取原有字段，因此不会因为新增字段而失效；新版前端可以同时显示最终题目进度和内部生成进度。

### 2. 合并后切割模式的阶段进度

建议将合并模式拆成以下阶段：

```text
planning      构造多人配音批次
submit        提交合并作品
download      下载合并音频
cut           按停顿切割逻辑片段
export        拼接题目并导出最终 MP3
package       ZIP 和历史记录整理
```

主进度仍以最终题目为基准，避免用户看到“只生成 1 个合并作品”而误以为任务已经全部完成。阶段内部可采用以下展示逻辑：

- `submit`：显示“合并作品 1/1 已提交”；
- `download`：显示“合并作品 1/1 已下载”；
- `cut`：显示“已切割 18/48 个逻辑片段”；
- `export`：显示“已整理 10/48 个最终音频”；
- `package`：显示“正在打包和保存历史记录”。

`processed` 可以继续使用 0 到最终题目总数之间的单调浮点数，切割和导出过程中逐步推进；前端显示时仍转换为整数，避免出现难以理解的小数题目数。

### 3. 单条生成模式的阶段进度

旧模式保持当前用户熟悉的展示方式：

```text
planning
→ batch-submit
→ batch-download
→ batch-export
→ package
```

日志示例：

```text
按原有方式逐段生成 — 已提交 20/48 个逻辑片段
已下载 18/48 个逻辑片段
正在整理第 10/48 个最终音频
```

该模式没有 `cut` 阶段，`sliced` 固定为 0 或等于逻辑片段完成数，避免把原有流程误显示为发生了切割。

### 4. 日志事件结构

继续沿用现有日志的 `stage`、`kind`、`key`、`title`、`detail` 和 `item` 字段，并增加可选字段：

```json
{
  "generation_mode": "composite_cut",
  "work_id": "composite-0001",
  "segment_id": "item-001::segment:2",
  "work_index": 1,
  "work_total": 1,
  "segment_index": 2,
  "segment_total": 48
}
```

合并模式日志至少包含：

- 已创建多少个合并作品；
- 每个合并作品包含多少逻辑片段；
- worksId 是否已捕获；
- 合并音频是否下载完成；
- 当前切割到哪个边界；
- 切割失败时的边界编号、文本预览和失败原因；
- 最终导出完成的题目数量。

示例：

```text
正在生成 1 个多人配音作品，包含 48 个逻辑片段
合并作品 1/1 已下载，开始安全切割
已切割 18/48 个逻辑片段
题目 10/48 已导出
```

### 5. 失败、取消和重连

- SSE 重连后，服务端需要重放当前模式、阶段和 `work`/`segments` 快照；
- 合并作品下载成功但切割失败时，任务进入明确的 `cut-error`，不能伪装成普通逐段生成失败；
- 单条模式失败时继续沿用当前逐段失败记录；
- 取消任务时，合并模式需要明确说明“已提交的合并作品不会重复提交，未切割部分不会继续导出”；
- 最终 `done` 事件增加 `generation_mode` 和合并作品数量，但保留原有 `completed`、`failed`、`total` 和文件数量字段。

## 十一、实施阶段

### 阶段一：网页能力验证（已完成）

使用登录后的讯飞页面测试一个包含两种音色、三个文本段的样例，抓取多人配音请求和结果。

重点确认角色节点、音色字段、参数字段、停顿节点和作品下载流程。

### 阶段二：合并生成（页面 UI 路径已接入）

实现多人配音批次构造、页面 UI 单次提交、统一下载和批次状态保存。连续相同配置优先一次选中设置，非连续配置分开处理。

### 阶段三：安全切割

实现停顿定位、首尾保护、人工停顿移除、片段数量校验和失败保护。

### 阶段四：接回现有交付流程

将切割结果接回现有题目音频、进度、历史记录和 ZIP 打包流程。

## 十二、测试范围

需要覆盖：

- 单音色文本；
- 两种音色交替；
- 三种及以上音色；
- W/M 标记和自定义角色；
- 文本首尾包含弱辅音、送气音和短词；
- 长文本接近 10000 字；
- 超过 10000 字后的批次拆分；
- 句号、引号、换行和特殊标点；
- 停顿边界不明显的情况；
- 下载失败、切割失败和任务中断恢复；
- 最终音频没有明显长首尾空档，也没有出现首音或尾音被裁掉的情况。

## 十三、验收标准

- 文本总量未超过网页限制时，只产生一个多人配音作品；
- 多音色顺序正确；
- 最终文件数量和原来一致；
- 每段没有明显的长首尾空档；
- 尾音、首音没有被裁掉；
- 切割失败时不会静默生成错误文件；
- 任务中断后不会因为重复提交造成额外扣次数；
- 现有结果页、历史记录和 ZIP 交付流程继续可用。

本方案已作为实现基准接入现有代码：默认使用“全部生成后切割”，并保留“单条单条生成”兼容选项；本文继续作为设计、断点恢复和验收标准留档。
