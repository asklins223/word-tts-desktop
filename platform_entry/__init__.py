"""外部平台页面录入（系统录入）统一包。

目录结构对齐其他顶层功能包（如 ``wordtts``）：可执行录入链路与
页面适配实现集中在这里，``scripts/`` 只保留与系统无关的构建/开发
CLI 工具。

- ``text_input``：课文录入。Word 文档解析 → 录入计划 → 可见页面执行。
- ``paper_input``：试卷录入。页面语义 JSON 的兼容 CLI 入口。
- ``adapter``：试卷/课文共用的页面适配实现（常量、规整、页面驱动、
  只读响应观察）。

所有模块只驱动可见页面控件；写操作一律通过页面提交，保存响应仅
被动记录。
"""