# 更新日志

## v2.7.15

### 修复多角色自定义音色切换与批量下载进度

- 修复多角色选择 `Linda-品质` 等非默认音色后，讯飞页面偶尔恢复平台默认音色，后续任务因错误命中本地缓存而跳过搜索的问题。
- 音色切换改为同时校验稳定音色 ID 与讯飞页面真实选中状态；页面状态不一致时会强制搜索并重新选择目标音色。
- 兼容讯飞音色卡片实际使用的蓝色边框选中标记，并收紧搜索结果的可见性和名称匹配，避免误选隐藏或相似音色。
- 保留多角色批量任务中的独立音色 ID，确保不同角色不会回退到默认男女声。
- 统一下载阶段按单条任务实时反馈下载和保存进度，前端进度条不再从 0% 直接跳到 100%。
- 增加非默认多音色、页面音色复位恢复、批量下载进度等回归测试。

## v2.7.14

### 修复 Windows 打包后渲染器崩溃

- 保留 Windows 系统默认语言回退所需的 Electron `en-US`、英文变体和简体中文语言资源。
- 修复打包应用创建窗口后 Chromium renderer 以 `0xC0000005` 访问冲突退出，导致 GitHub Actions 冒烟测试超时或失败的问题。
- 已通过真实 Windows 打包验证：后端、Electron 应用启动、渲染器就绪、安装包查找和产物上传全部成功。

## v2.7.13

### 修复 Windows 冒烟模式渲染器崩溃

- 根据 Windows runner 诊断日志定位到隐藏 `BrowserWindow` 创建后 Chromium renderer 以 `0xC0000005` 访问冲突退出的问题。
- Windows 冒烟模式改为使用可见窗口，避免隐藏窗口触发渲染器崩溃；正常用户运行逻辑不变。

## v2.7.12

### 修复 Windows 工作流脚本格式

- 修正 Windows 冒烟测试 PowerShell 多行脚本的 YAML 缩进，恢复 GitHub Actions 正常解析和执行。

## v2.7.11

### 修复 Windows Electron 冒烟探测无响应

- 为渲染器冒烟探测增加单次 JavaScript 执行超时，避免 Windows 下页面无响应时主进程永久等待。
- 增加 45 秒冒烟 watchdog，失败时自动清理并退出，不再让 GitHub Actions 无日志等待 120 秒。
- 冒烟模式禁用 GPU 合成，减少 Windows runner 虚拟显示驱动导致隐藏窗口不返回的问题。
- 将冒烟阶段、后端启动/退出、渲染器状态和失败堆栈写入系统临时目录 `wordtts-electron-smoke.log`，并在 Windows 工作流超时或失败时输出诊断日志。
- Windows 工作流超时清理改为递归结束 Electron 进程树，避免残留子进程影响后续步骤。

## v2.7.10

### 修复 Windows 打包冒烟测试超时

- 修复 Windows 下打包 Electron 退出时后端进程树未及时触发 `exit` 事件，导致 `--smoke-test` 反复进入退出清理流程并最终超时的问题。
- 后端关闭流程改为幂等 Promise；Windows 超时后使用 `taskkill /T /F` 清理后端及其 Playwright/Chromium 子进程，并增加最终退出兜底。
- 防止重复 `will-quit` 事件再次启动清理循环，避免 GitHub Actions 等待 120 秒后失败。

## v2.7.9

### 优化讯飞浏览器自动化与延迟页面兼容

- 将讯飞确认合成、AI 标识、作品设置和订单状态探测合并为单次页面状态快照，减少同一轮重复 DOM 扫描。
- 保留并增强自适应轮询：页面首次未挂载时不会立即判定失败，轮询间隔逐步退避但保持持续探测；确认合成、AI 开关和弹窗关闭等待窗口覆盖页面延迟 5-10 秒的情况。
- 精确识别“AI 标识”所在的 `role="switch"`，避免误操作其他开关；自动勾选“不再提示”、点击确认，并回读弹窗关闭结果。
- 将后续确认流程也改为统一状态探测，避免 AI 弹窗、额度错误、登录失效和订单状态互相误判。
- Playwright Sync API 的会话健康检查、登录、合成、下载全部固定到专用线程，避免 asyncio 事件循环与 greenlet 跨线程异常。
- 保持按精确 `worksId` 匹配作品、进入下载页批量勾选并统一下载，避免同名或最新作品错配。

### 降低生成和渲染过程的资源占用

- 进度文件保存改为后台线程节流写入，统计状态改为增量维护并限制事件推送频率，减少大文档生成时的磁盘 I/O、JSON 序列化和 O(n²) 扫描。
- 多角色音频拼接在音频参数一致时直接合并原始 PCM 数据，MP3 导出后只做轻量文件头校验，移除重复的 FFmpeg 回读和反复音频复制。
- 结果列表使用 `DocumentFragment` 批量挂载，波形占位条由 64 个减少为 32 个，启用 `content-visibility` 和布局隔离。
- 音频列表搜索采用 `requestAnimationFrame` 合帧；根据设备 CPU/内存启用低性能模式，关闭非必要装饰动画但保留生成进度反馈。
- 普通运行不再将后端每条 stdout 日志复制到 Electron 主进程控制台，同时仍持续消费管道避免阻塞；开发调试可通过 `--dev` 或 `WORDTTS_DEBUG_LOGS=1` 恢复日志转发。

### 精简桌面端打包体积

- Chromium 仅保留中文简体和英文语言资源。
- 移除不参与讯飞网页自动化的 Widevine 目录及 Playwright 的类型、协议和 Vite 开发期资源。
- 保留 Playwright driver、Chromium 和 Electron 运行所需文件，兼容 macOS 与 Windows 打包流程。

### 测试与回归保障

- 增加真实 Ant Design AI 标识/作品设置 DOM 回归测试。
- 增加页面首次探测为空、后续延迟挂载 AI 弹窗仍能被捕获的测试。
- 增加 MP3 快速校验、音频拼接、配置隔离和渲染器配置回归测试。

## v2.7.6

### 修复讯飞生成线程与 AI 标识确认

- 讯飞 Playwright Sync API 统一固定到专用单线程执行器，修复 Electron 任务中偶发的 `Cannot switch to a different thread`。
- AI 标识弹窗兼容真实 `.ant-modal-content` 结构；未成功勾选“不再提示”或确认、以及确认后弹窗未关闭时，任务会明确失败并记录弹窗状态。
- 收紧 AI 弹窗关键词，避免把仍在显示的“作品设置 / AI 标识”误判为 AI 说明弹窗。
- 修复本机 `npm run build:python` 在没有全局 `pyinstaller` 命令时无法构建的问题。

## v2.7.5

### 修复音色头像与讯飞会话线程问题

- 修复音色头像与首字母 fallback 并排渲染的问题；首屏头像直接加载，列表滚动时按可见区域加载其余头像。
- 修复已有讯飞 Playwright 会话在 asyncio 事件循环线程执行 Sync API 健康检查的问题，并消除首次并发创建重复浏览器会话的竞态。
- 针对讯飞 AI 标识说明弹窗的真实 Ant Design DOM 增加回归验证，确保勾选“不再提示”后点击“确认”。

## v2.7.4

### 修复音色选择与讯飞弹窗流程

- 音色列表改为按实际可用宽度自适应排列，避免被详情面板挤压；窄窗口自动切换为单列布局。
- 扩展讯飞 AI 标识弹窗识别，兼容不同 dialog、复选框和按钮结构，增加 Playwright locator 兜底并记录失败时的弹窗快照。
- 兼容部分讯飞页面首次确认后直接提交、不会再次显示“确认合成”的情况，避免误判失败后重复提交触发频控。

## v2.7.3

### 统一迁移至讯飞配音

- WordTTS 生成链路统一使用讯飞配音，移除 Edge TTS、TTSMaker 及其训练/回退代码。
- 女声固定为 Amanda，男声固定为 George；单词和例句不再使用独立音色，统一使用默认女声 Amanda。
- 语速、语调、音量统一为讯飞 0-100 整数参数，默认均为 50；前端提供滑块和数字输入框双向同步，后端也会强制校验范围。
- 讯飞返回的 MP3 不再执行首尾裁切、尾部补静音或应用层段落停顿拼接，保留原始音频内容。
- 默认男声、默认女声和文档角色改为独立保存三项参数，即使选择同一个音色也不会互相覆盖。
- 修复 AI 标识弹窗流程：自动勾选“不再提示”、确认并等待二次“确认合成”；文本改为一次性粘贴式输入。

### 兼容 Section A/B 课文跟读新格式

- 新格式通过 `Section`、跟读子题型和有效内容的结构组合判定，不再因单独出现 `Section A`、文件名或年级名而误切换；历史 `Understanding Idea/Reading for writing` 规则继续保留。
- 显式 `Conversation` 对话按角色边界拆分并把角色元数据交给音色配置；普通多角色段落保留为一个音频，多个 Conversation、角色名和 `SA/SB` 文件命名均保留。
- 文章按大标题、段落和“小标题 + 紧邻段落”生成音频，兼容 `//` 分割标记；结构化识别的 `Reading Plus` 使用 `RP` 命名前缀。

### 整理项目资源与运行路径

- 解析示例文档和结果统一移至 `examples/`，讯飞音色种子目录统一移至 `resources/`。
- 统一源码、Electron 开发版和 PyInstaller 版的资源/用户数据路径；音色缓存、音频输出和登录状态不再写入只读打包资源。
