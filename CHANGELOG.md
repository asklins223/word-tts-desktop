# 更新日志

## 2.0.0 - 2026-07-29

### 成熟落地版

本版本将自 `25d6712c41edc4af4d15b9e657691f624287abd6` 开始、此前分散在多个临时版本与修补提交中的功能统一整理为正式的 2.0 版本。以下是这段 1.x → 2.0 演进区间的累计能力汇总，并非都由最后一次发布提交单独引入；内容均按实际代码归纳。

### 产品与桌面体验

- 产品品牌统一升级为“小猪wordTTS”，同步窗口标题、侧栏品牌、应用图标、安装包、快捷方式、构建产物和 GitHub Release 名称。
- 重新设计 Electron 桌面界面，形成“导入文档 → 核对与设置 → 生成音频 → 试听与下载”的清晰四步工作流，并完善紧凑窗口下的响应式布局。
- 重构上传、配置、生成、结果和状态栏的信息层级，增加任务摘要、错误恢复入口、部分失败提示以及更明确的操作反馈。
- 新增统一 UI 组件层，替换设置页和结果页的 8 个系统下拉框；支持方向键、Home/End、回车、空格、Esc、快速输入定位、动态选项同步和弹层自动避让。
- 保存配置、删除预设、删除历史、更换文档、中止任务和应用错误统一使用应用内对话框；危险操作默认聚焦取消，并支持焦点锁定、关闭后恢复与消息排队。
- 修复全局样式重置导致应用对话框贴在窗口左上角的问题；对话框现在显式按视口双轴居中，并在最小窗口与窗口缩放后保持正确位置。
- 修复新建任务后无法弹出系统 Word 文件选择框、结果页与历史页无法弹出系统保存框的问题；原生对话框现在绑定实际发起操作的窗口，所有失败分支返回明确中文反馈，不再显示 `unknown`。
- 增加减少动态效果和系统高对比模式的基础适配，完善键盘操作与 ARIA 语义。

### 任务闭环与历史记录

- 新增本机历史中心，任务完成后自动保存结构化清单，可跨应用重启查看文档、生成范围、格式、成功/失败数量和音频明细。
- 支持从历史记录重新进入结果页、逐条试听与下载、下载 ZIP、删除记录，并在文件缺失时保持记录可见和可清理。
- 历史记录采用原子写入、稳定 ID、目录边界校验和最多 20 条自动淘汰策略，避免损坏清单或删除任务目录之外的文件。
- 会话清理与历史归档解耦：结束当前任务时释放运行状态，同时保留已归档结果；归档操作具备幂等性。
- 结果页新增文件名/内容搜索、题型筛选、真实音频播放和按可见区域延迟加载波形，降低大量结果同时解码的资源占用。

### 生成过程与可观测性

- 生成页升级为结构化任务时间线，按准备、解析、合成、打包、归档和完成阶段展示状态、单条耗时、音色、题型、文本摘要与失败原因。
- 时间线支持阶段筛选、自动跟随、未读提示、折叠详情和有界 DOM，长任务不会无限增长页面节点。
- SSE 改为带递增序号的有界事件日志，每个连接独立维护游标，支持多客户端并行订阅、断线重放和最终状态恢复。
- 统一成功、失败、取消和结束事件的终态协议，修复最后一条处理期间取消仍可能误报完成、异常路径缺少结束事件等竞态问题。
- 重连快照保持进度单调，并可恢复最终下载信息、错误摘要、取消原因和历史记录 ID。
- 增加任务取消、重试服务、失败恢复和新建任务保护，未保存结果或正在生成时会给出明确风险说明。

### 音频生成与男声音色链路

- 修复女声切换男声时停顿明显偏大的问题，使不同切换方向使用一致的间隔策略，并去除 TTS 片段边缘的多余静音。
- 男声继续使用 TTSMaker 788 Alfie，女声使用 edge-tts；男声服务不可用时自动回退到 Remy，并在时间线中明确显示原因。
- 精简 TTSMaker 过时的验证码/OCR 依赖，降低运行与打包复杂度；无窗口回退改用完整 Chromium 的新版无头模式。
- 增加生成计划、音色引擎状态、逐条处理、打包和归档的结构化日志，使失败定位不再依赖原始控制台输出。

### 安全、隔离与数据兼容

- 每个任务使用独立且不可碰撞的输出目录，并通过源文件指纹、路径规范化和允许列表阻止跨会话读取或目录穿越。
- 历史下载接口只允许清单内文件；Electron 文件打开、保存与定位操作继续经过主进程路径校验。
- 后端启动失败和异常退出改为受限的单向 IPC 通知，由 renderer 使用纯文本渲染；renderer 自身不可用时保留原生灾难恢复提示。
- 原生文件 IPC 仅接受当前主窗口、固定本地页面和主 frame 的请求，并阻止界面导航到非预期地址或打开新窗口，避免页面切换后继续持有本机文件能力。
- 品牌改名后继续沿用原 `WordTTS` 用户数据目录，同时保留 `appId`、API Header、环境变量、LocalStorage Key 和健康检查标识，确保已有历史、预设和升级识别不受影响。
- 用户数据目录不可写时安全回退到 Electron 默认目录，避免应用在创建窗口前直接退出。
- 修复 macOS 启动等待期间可能重复创建窗口、旧窗口关闭后误清空当前窗口引用的竞态，并保证窗口重建与 Dock 激活始终复用唯一主窗口。

### 打包与发布工程

- 固定 Electron、Playwright 与 Python 构建依赖，精简 PyInstaller 隐式依赖和无用模块，减少重复运行时与不可控版本漂移。
- 打包时按 Playwright 清单校验并复制准确的 Chromium revision，只保留中文和英文 locale；复用 Electron 内置 Node 启动 Playwright driver。
- macOS 构建增加架构一致性、最低系统版本、嵌套 Chromium 签名和打包后冒烟检查，并兼容未配置签名证书的本机构建。
- Windows 构建适配新的中文产品名与产物路径，并统一打包后端 stdout/stderr 的 UTF-8 输出，避免中文日志触发 `UnicodeEncodeError`。
- macOS/Windows Workflow 的版本写入改为幂等操作；目标版本已一致时跳过 `npm version`，并清理空签名环境变量造成的误判。
- CI 保留后端健康检查、内置 Playwright/Chromium 冒烟测试，并让打包后的 Electron 实际加载 preload、UI 组件与本地服务完成桌面界面端到端冒烟；发布路径已全部适配“小猪wordTTS”。

### 测试与质量保障

- 扩充桌面后端测试，覆盖会话隔离、路径越界、SSE 多连接与重放、取消终态、进度单调性、历史归档/删除/淘汰和缺失资源处理。
- 增加音频边缘静音与男女声切换间隔回归测试。
- UI 组件采用渐进增强方式保留原生 `<select>` 作为业务状态源，动态预设、格式/质量联动和题型筛选无需重写现有业务逻辑。
- 新增 Electron 原生 Open/Save 对话框契约测试，22 项测试与子测试覆盖调用窗口绑定、窗口恢复、用户取消、窗口缺失、来源校验、路径校验、文件缺失、系统对话框异常与复制失败等分支。
