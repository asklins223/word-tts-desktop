# 更新日志

## v3.0.12

- 安装界面焕然一新（小猪搬家工具箱，启动！）
- 修复了一些问题（能够遇见的bug又少了）
- 优化了生成音频的稳定性～（虽然可能碰不到）
- 完善 Windows 自绘安装器无窗口运行的阶段日志与异常诊断。

## v3.0.11

- 安装界面焕然一新（小猪搬家工具箱，启动！）
- 修复了一些问题（能够遇见的bug又少了）
- 优化了生成音频的稳定性～（虽然可能碰不到）
- 增加 Windows 自绘安装器生命周期冒烟的进程日志采集，便于定位异常。

## v3.0.10

- 安装界面焕然一新（小猪搬家工具箱，启动！）
- 修复了一些问题（能够遇见的bug又少了）
- 优化了生成音频的稳定性～（虽然可能碰不到）
- 修复 Windows runner 上 30ms 临时上传 TTL 测试受文件句柄初始化时序影响而偶发失败的问题。

## v3.0.9

- 安装界面焕然一新（小猪搬家工具箱，启动！）
- 修复了一些问题（能够遇见的bug又少了）
- 优化了生成音频的稳定性～（虽然可能碰不到）
- 修复 Windows 自绘安装器构建时覆盖主应用 `win-unpacked` 目录的问题。

## v3.0.8

- 安装界面焕然一新（小猪搬家工具箱，启动！）
- 修复了一些问题（能够遇见的bug又少了）
- 优化了生成音频的稳定性～（虽然可能碰不到）
- 修复 Windows 使用 Node 24 构建自绘安装程序时无法启动 `npx.cmd` 的问题。

## v3.0.7

- 安装界面焕然一新（小猪搬家工具箱，启动！）
- 修复了一些问题（能够遇见的bug又少了）
- 优化了生成音频的稳定性～（虽然可能碰不到）
- 修复 Windows 卸载时旧注册表项缺失导致清理脚本误报失败的问题。

## v3.0.6

- 安装界面焕然一新（小猪搬家工具箱，启动！）
- 修复了一些问题（能够遇见的bug又少了）
- 优化了生成音频的稳定性～（虽然可能碰不到）
- 修复打包冒烟启动时首屏配置等待联网音色目录，离线也能顺利打开应用。
- 修正 Windows 安装器集成测试对真实 PE 文件的断言。

## v3.0.5

- 安装界面焕然一新（小猪搬家工具箱，启动！）
- 修复了一些问题（能够遇见的bug又少了）
- 优化了生成音频的稳定性～（虽然可能碰不到）
- 修复 Windows 安装器快捷方式脚本生成错误，中文路径这次真的能走通了。

## v3.0.4

- 安装界面焕然一新（小猪搬家工具箱，启动！）
- 修复了一些问题（能够遇见的bug又少了）
- 优化了生成音频的稳定性～（虽然可能碰不到）
- 修复 Windows 中文路径快捷方式创建失败的问题，桌面和开始菜单终于能认出小猪了。

## v3.0.3

- 安装界面焕然一新（小猪搬家工具箱，启动！）
- 修复了一些问题（能够遇见的bug又少了）
- 优化了生成音频的稳定性～（虽然可能碰不到）
- 修复 Windows/macOS 发布打包兼容性，安装包这次不再临场发挥。

## v3.0.2

- 安装界面焕然一新（小猪搬家工具箱，启动！）
- 修复了一些问题（能够遇见的bug又少了）
- 优化了生成音频的稳定性～（虽然可能碰不到）

## v3.0.1

- 修复了一些问题，新加了更多的试卷解析，现在更好用了～

## v3.0.0

- 支持试卷解析咯～

更新了亿点点东西～

1. 外观全换了（整容级，不骗你）
2. 内在也换新了（燃尽了）
3. 更新会弹窗提醒，点一下就开始下载（半自动，主打一个尊重）
4. 新题型顺手加了一批

饿了，去码头整点薯条～

## v2.7.45

- 修复“终止任务后无法再生成”的死胡同：任务被终止/断网留下未决外部副作用时，配置页点击生成现在返回可路由的 `RECONCILIATION_REQUIRED` 并自动弹出“确认未提交后重试”对账面板；新增 `/api/v1/workflows/{id}/recovery` 端点，应用重启后也能重建对账目标（作品名一并展示），确认未提交即可继续生成，文档不会因网络问题永久卡死。
- 修复打包崩溃：`electron/source-staging.js` 此前未加入 electron-builder 的 `build.files` 清单，安装包启动即报 "Cannot find module './source-staging'"；已补入清单并新增打包清单覆盖回归测试（main/preload 的每个本地依赖必须在清单内）。
- 打包自检加固：`--smoke-test` 模式下未捕获异常此前只写日志被吞掉，坏包也能通过构建冒烟；现在冒烟模式遇未捕获异常立即以非零码退出，构建脚本会明确失败。
- 按产品决定物理删除 server.py 内的旧会话引擎与全部旧 `/api/*` 路由（4074→646 行）：未迁移路径由中间件统一返回 `410 API_VERSION_RETIRED`，`/api/v1` 工作流挂载、音色目录与音色资产缓存、打包启动逻辑不受影响。
- 拖拽导入改走主进程分块暂存：渲染层每次只持约 4MB 分块，文档由主进程在允许目录内落盘后按一次性句柄流式上传，300MB 级文档不再整块进入渲染进程内存；乱序/越界/TTL/退出均 fail-closed 并物理清理暂存文件。
- 渲染层 Store 新增有界 workspace 进度投影：只有通过顺序校验与去重的事件才推进阶段、分段计数与运行时消息，进度 UI 的权威数值改为订阅回调从投影渲染，断线重连或快照重同步后自动回到最新值。
- 讯飞停顿插入新增“光标折叠到行尾”快速路径优先级回归：工具栏直带时长按钮的页面主路径完成插入时，重型兜底（原生选区 + 整页扫描 + 菜单路径）零调用。
- 语音头像/试听样本读取增加 8MB 显式上限（代理层 16MB 之内再设资源级边界）。

## v2.7.44

- 修复解析完成后后台恢复调度器误把配置页的 `PREPARING` 状态当成已确认生成，导致用户尚未点击生成就启动讯飞自动化并触发配置冻结。
- 生成启动遇到并发接受时改为接管权威任务；生成页新增停止入口，返回配置前会等待取消和本地浏览器清理完成，避免任务在后台隐藏运行。
- 修复新版讯飞停顿菜单点击后选区丢失的问题，恢复原脚本“选中正文 → `ArrowRight` 折叠到行尾 → 点击真实停顿控件”的流程，并校验目标停顿标签。
- 自动重试调度器现在要求工作流存在用户确认生成的 `WORKFLOW_GENERATE` 事件才允许无人值守重试；历史接管产物遗留的 `WAITING_RETRY` 任务不会再自行重开讯飞浏览器，只会等待用户显式重试。
- 停顿插入新增“光标折叠到行尾”快速路径：一次页面求值完成编辑器聚焦、选区定位与原生光标折叠，再一步点击时长控件，单处停顿的 Playwright 往返回到原脚本水平；失败时依次降级到原脚本选区契约与原生 `select_text` 兜底，插入结果仍由逐行回读校验兜住。
- 发布门新增显式豁免机制：真实账号 smoke 与目标设备性能现场证据按产品决定豁免并在报表留痕；`--no-waivers` 可随时恢复严格门。

## v2.7.43

- 修复正式 Electron App 启动后真实讯飞 Provider 被默认关闭的问题；安装包现在开箱启用真实生成和自动重试，不再要求额外设置环境变量。
- 保留 `--smoke-test` 和逻辑 smoke 的离线隔离，并提供 `--disable-real-provider` 作为显式离线诊断选项。

## v2.7.42

- 修复多人配音合并模式没有实际插入停顿标签的问题：兼容讯飞页面的“2秒”文案、先点“停顿”再选时长的菜单结构，以及 `data-value`/`data-duration` 等页面标记形式。
- 停顿插入改为使用编辑器原生选区并在工具栏/菜单操作后恢复选区，插入完成后逐段回读校验；停顿控件未找到或标记缺失时明确失败，不再带着无边界标记提交作品。
- 保留 `single_segment` 兼容模式不插入停顿；默认 `composite_cut` 仅在题目之间插入用于安全切割的停顿标记。

## v2.7.41

- 多人配音音色目录切换为讯飞网页实际使用的 `speaker/common/list` 基础音色列表，不再把右侧栏的 `Pro` 等变体作为可选音色。
- 固定 `qry_tags` 标签快照，补全 14 个父类和 44 个父/子标签，并保留标签层级及元数据；筛选栏不再截断标签或隐藏零数量标签。
- 默认女声统一为英语-Amanda（`commonId: 10001009`），默认男声统一为英语-George（`commonId: 10001089`），同时兼容旧版已保存的音色 key。

## v2.7.40

- 修复脚本自动化在第二个音色时停顿 2-4 秒并重新选区的问题：`Amanda` 同时命中 `Amanda-教育` 等变体导致 `_find_composite_voice_card` 候选不唯一，原逻辑在轮询中吞掉异常并等待至超时；现按主名称 `p/strong` 精确匹配并以长度启发式快速择优，卡片查找即时返回。
- 收敛多人配音等待：卡片轮询 5s→4s、队列计数 5s→3s、选卡后固定等待 180ms→80ms、入队 35ms→20ms，减少第二音色切换的感知停顿。
- 保持“多人配音”面板作用域隔离，避免右侧栏搜索框被误用。

## v2.7.39

- 修复“多人配音”面板与右侧音色栏同时存在同名搜索框时，脚本可能复用右侧栏搜索框导致音色选择被拦截的问题；面板搜索与卡片查找均收敛至弹层作用域。

## v2.7.36

- 修复 Windows 桌面端冒烟测试在网络不可用或讯飞音色接口响应较慢时，一直停留在“正在连接服务”并导致打包失败的问题。
- 配置接口先使用内置/本地音色目录快速返回，在线目录改为后台刷新；Windows 工作流清理 Playwright 环境变量，避免污染 Electron 主进程。

## v2.7.35

- 修复「听后选择」音频边界：每一块「录音原文」生成一个音频，块内 W/M 仍按角色切换音色，不再按角色拆成多个文件。
- 递增解析缓存版本，避免复用旧的按角色拆分结果。

## v2.7.34

- 修复讯飞提交成功但未捕获 worksId 时进入通用重试、可能重复提交并重复扣费的问题。
- 增加稳定作品名对账、临时 worksId 保护、多人配音/断点续跑状态持久化和批量取消协作。

## v2.7.33

- 修复批量合成同时捕获临时与正式 worksId 时的错配，并在多文件下载事件无法证明归属时拒绝按顺序写错音频。
- 修复多人配音触发频控后未恢复页面就重试的问题；macOS 打包启动会先配置内置 ffmpeg，并复用已验证的后端就绪状态。

## v2.7.32

- 多人配音正常路径改用当前页面 Range 精确选中每个正文行，再通过真实 Command/Ctrl pointerup 加入讯飞多段队列；正文回读或队列数量异常时自动切回原生选区，避免误套音色。
- 停顿标签优先直接定位可见的讯飞停顿按钮并真实点击，减少长文档逐处查询；仍保留逐处插入和批量回读校验，确保首尾边界不漂移。

## v2.7.31

- 多人配音工具栏和音色卡片改为批量读取可见控件后再执行真实点击，减少客户端 DOM 往返。
- 音色面板参数与停顿标签改为操作后快速回读，正常路径不再固定等待；面板暂态未就绪时只短轮询恢复。
- 增加配置组和停顿批量耗时日志，便于确认真实页面操作阶段是否异常变慢。

## v2.7.30

- 多人配音选区改为轻量滚动 + 原生选区回读驱动等待，减少每行固定等待；仅在讯飞页面事件未落地时补发一次真实 pointerup。
- 停顿标签改为等待按钮和标记实际出现后继续，缩短正常路径等待，同时保留逐处定位与插入校验。

## v2.7.29

- 多人配音按最终音色、语速、语调、音量配置收集不连续段落，使用讯飞网页真实的 Command/Ctrl 多段选择队列，同一配置组一次性标注男声或女声，不再全选后逐段修正。
- 长文档滚动后改用网页保留的待处理选区装饰节点回读队列状态，避免误判队列为空而退回慢方案；保留连续区间回退和每行正文精确选区校验。
- 停顿统一放到音色标注完成后的收尾阶段，按段落末尾通过网页按钮插入并逐处回读，日志改为批量汇总，避免刷屏。

## v2.7.28

- 修复多人配音重试已有失败进度时，任务刚启动会短暂显示上一轮失败数量和 99% 进度的问题。
- 重试前先清理上一轮失败项的本轮统计，同时保留合并作品边界和断点信息。

## v2.7.27

- 修复多人配音在已标注段落上重新选区时可能把音色标签当作正文起点的问题。
- 收紧音色卡片选择与标注回读：同名候选不唯一时停止，逐行校验唯一 speaker 标记、讯飞 speaker-id、语速/语调/音量和正文，避免误套用未配置音色。

## v2.7.26

- 修复多人配音基准音色标注后，后续选区校验把 `Amanda-教育` 等不可编辑音色标签误算为正文，导致整批对话全部失败的问题。

## v2.7.25

- 优化多人配音音色标注：先用高频配置覆盖全文，再只修正例外区间，减少交错对话在客户端中的重复面板操作；全文选区异常时保留连续区间回退。
- 修复生成题目全部导出后，ZIP 打包和历史归档尚未完成却提前显示 100% 的问题；新增整理、归档阶段进度。
- 合并生成日志改为作品/阶段汇总，失败内容仍保留逐项记录，减少长文档日志刷屏。

## v2.7.24

### 加固长内容多人音频切割

- 合并作品增加单作品 120 条题目的安全上限；超长内容按完整题目拆分，避免切割候选过多导致处理变慢或边界选择不稳定。
- 切割优先校验接近页面 2 秒停顿的强标记；标记不足或候选有歧义时明确失败并保留合并音频，不再静默按自然停顿猜测边界。
- 进度页和日志返回切割策略、候选数量、强标记数量及选中边界位置，便于定位首尾音和长内容错切问题。
- 递增音频算法与后端协议版本，避免旧客户端或旧切割结果混用。

## v2.7.23

### 修复听后选择对话分段与多人音频切割

- 听后选择按说话人轮次生成独立音频：`W:` 和 `M:` 各自成为一条音频，同一说话人连续续行合并；不再把整段对话拼成一条。
- 多人合并音频切割改为全局选择停顿候选，优先使用页面插入的长停顿标记，避免前面误选自然停顿后导致后续第三段及之后整体错位。
- 切点改用静音核心区中心，保留首音、尾音保护；同步递增解析缓存、音频算法和后端协议版本，旧任务不会复用错误分段结果。
- 增加真实 MP3 静音边界、长停顿优先和听后选择说话人分段回归测试。

## v2.7.22

### 修复客户端多人配音批量标注变慢与打包后端不同步

- 修复打包客户端使用旧后端时仍能启动、随后逐条标注的隐性回退；前后端协议不一致时现在会在启动阶段明确提示重新安装完整版本。
- 多人配音连续相同音色和参数的行优先使用真实页面 Shift-click 批量选区，并保留鼠标拖选和页面选区兜底；所有选区都通过文本精确回读，禁止失败后拆成逐行设置。
- 没有显式传入生成方式的请求遵循产品默认「全部生成后切割」，原有「单条单条生成」仍可显式选择。
- `--electron` 打包入口改为先重建当前 Python 后端，避免把磁盘中的旧 PyInstaller 产物装入新客户端。
- 增加长编辑器跨滚动选区、默认生成方式、后端协议版本回归测试，并同步客户端版本号。

## v2.7.21

### 多人配音改为真实页面操作

- 多人合并作品改为通过讯飞可见编辑器输入文本、选择“多人配音”、设置音色和参数后点击页面“生成音频”，不再直接调用多人提交或订单生成接口。
- 连续且音色、语速、语调、音量完全相同的行会批量选中并统一设置；非连续配置分开处理，拖选校验失败时安全降级为逐行设置。
- 每次音色套用前校验浏览器实际选区，点击“使用”后回读 speaker 标记；题目边界通过页面停顿按钮插入，并继续交给既有安全切割算法处理首尾保护。
- 保留原有“单条单条生成”模式和既有 worksId、下载、进度及断点流程。

## v2.7.20

### 修复讯飞多人配音参数非法

- 音色目录的具体 `speakerNo` 在多人配音提交前按讯飞网页流程反查对应 `commonId`，并在当前会话内缓存。
- 目录暂时缺少或反查失败时使用网页兼容的 `commonId=0`，不再向接口发送 `null`，修复「999989：参数不合法」。
- 增加多人配音 commonId 反查和缺省值回归测试，并完成真实登录会话只读烟测。

## v2.7.17

### 修复生成进度、结果校对与题型命名

- 生成、下载、整理阶段的进度计数统一按整数显示，采用四舍五入并限制在文档总数内，修复前端和状态消息出现小数的问题。
- 修复结果页音色元数据兼容：支持 `voice_keys`、`voice_key` 和历史 `voice` 字段，处理空值、重复值及大小写差异，确保音色头像、名称和试听按钮能正确回退与显示。
- 生成完成后等待音色头像和示例音频缓存请求，并只更新结果列表中的音色条，避免缓存完成时重建音频和原文状态；相同音色仍只缓存一次。
- 修复生成页在小窗口、长任务日志和展开详情时无法上下滚动的问题，确保主内容与日志都能完整查看。
- 语篇跟读在显式 `Conversation x` 场景下按 `SA/SB-语-Cx-y` 命名；段落跟读继续按 `SA/SB-段-Cx-y` 命名，没有 Conversation 时保持原有命名规则，并递增解析器版本避免复用旧解析结果。
- 同步递增源文档解析缓存版本，旧 `parsed.json` 不会绕过新命名规则继续复用。
- 增加空音色字段、大小写兼容、整数进度、音色缓存和多 Conversation 命名的回归测试。

## v2.7.16

### 完善题型兼容、批量交付与结果页校对

- 新增「听后选择」题型解析：只提取「录音原文」中的对话，`W/w` 使用女声、`M/m` 使用男声，题干、选项和计算机提示不会进入配音内容；同一文档可与旧题型混排并分别识别。
- 完善 Section A/B 课文跟读的结构判断，只有 Section、子题型和有效正文形成连续结构时才启用新规则；显式 Conversation 段落按 `SA-段-Cx-y` 命名，并支持同一文档中的多段对话。
- 批量生成按音色和语速、语调、音量分组提交，统一进入作品下载页；优先使用精确 `worksId` 对应的签名地址下载，浏览器事件兜底时按作品名/ID匹配，禁止乱序下载造成音频错配。
- 增加提交、下载、整理阶段的连续进度反馈，重构生成页任务控制台；修复 Python 3.9 的 SSE 事件循环绑定和测试兼容问题。
- 结果页默认展开原文，增加每条音频实际使用音色的头像、名称和试听按钮（全局互斥播放），并增加回到顶部按钮；生成时缓存音色头像与示例音频，避免相同音色重复缓存。
- 长期配置只保存默认男女声及其独立参数，文档角色音色和参数保持当前任务级别；继续固定输出 MP3，保留 0-100 滑块与输入框联动。
- 增加听后选择、音色缓存、批量下载 ID 匹配、签名下载、进度时间线和 Electron 结果页回归测试。

## v2.7.15

### 修复多角色自定义音色切换与批量下载进度

- 修复多角色选择 `Linda-品质` 等非默认音色后，讯飞页面偶尔恢复平台默认音色，后续任务因错误命中本地缓存而跳过搜索的问题。
- 音色切换改为同时校验稳定音色 ID 与讯飞页面真实选中状态；页面状态不一致时会强制搜索并重新选择目标音色。
- 兼容讯飞音色卡片实际使用的蓝色边框选中标记，并收紧搜索结果的可见性和名称匹配，避免误选隐藏或相似音色。
- 保留多角色批量任务中的独立音色 ID，确保不同角色不会回退到默认男女声。
- 统一下载阶段按单条任务实时反馈下载和保存进度，前端进度条不再从 0% 直接跳到 100%。
- 增加非默认多音色、页面音色复位恢复、批量下载进度等回归测试。

## v2.7.14

### 修复 Windows 打包后渲染器崩溃

- 保留 Windows 系统默认语言回退所需的 Electron `en-US`、英文变体和简体中文语言资源。
- 修复打包应用创建窗口后 Chromium renderer 以 `0xC0000005` 访问冲突退出，导致 GitHub Actions 冒烟测试超时或失败的问题。
- 已通过真实 Windows 打包验证：后端、Electron 应用启动、渲染器就绪、安装包查找和产物上传全部成功。

## v2.7.13

### 修复 Windows 冒烟模式渲染器崩溃

- 根据 Windows runner 诊断日志定位到隐藏 `BrowserWindow` 创建后 Chromium renderer 以 `0xC0000005` 访问冲突退出的问题。
- Windows 冒烟模式改为使用可见窗口，避免隐藏窗口触发渲染器崩溃；正常用户运行逻辑不变。

## v2.7.12

### 修复 Windows 工作流脚本格式

- 修正 Windows 冒烟测试 PowerShell 多行脚本的 YAML 缩进，恢复 GitHub Actions 正常解析和执行。

## v2.7.11

### 修复 Windows Electron 冒烟探测无响应

- 为渲染器冒烟探测增加单次 JavaScript 执行超时，避免 Windows 下页面无响应时主进程永久等待。
- 增加 45 秒冒烟 watchdog，失败时自动清理并退出，不再让 GitHub Actions 无日志等待 120 秒。
- 冒烟模式禁用 GPU 合成，减少 Windows runner 虚拟显示驱动导致隐藏窗口不返回的问题。
- 将冒烟阶段、后端启动/退出、渲染器状态和失败堆栈写入系统临时目录 `wordtts-electron-smoke.log`，并在 Windows 工作流超时或失败时输出诊断日志。
- Windows 工作流超时清理改为递归结束 Electron 进程树，避免残留子进程影响后续步骤。

## v2.7.10

### 修复 Windows 打包冒烟测试超时

- 修复 Windows 下打包 Electron 退出时后端进程树未及时触发 `exit` 事件，导致 `--smoke-test` 反复进入退出清理流程并最终超时的问题。
- 后端关闭流程改为幂等 Promise；Windows 超时后使用 `taskkill /T /F` 清理后端及其 Playwright/Chromium 子进程，并增加最终退出兜底。
- 防止重复 `will-quit` 事件再次启动清理循环，避免 GitHub Actions 等待 120 秒后失败。

## v2.7.9

### 优化讯飞浏览器自动化与延迟页面兼容

- 将讯飞确认合成、AI 标识、作品设置和订单状态探测合并为单次页面状态快照，减少同一轮重复 DOM 扫描。
- 保留并增强自适应轮询：页面首次未挂载时不会立即判定失败，轮询间隔逐步退避但保持持续探测；确认合成、AI 开关和弹窗关闭等待窗口覆盖页面延迟 5-10 秒的情况。
- 精确识别“AI 标识”所在的 `role="switch"`，避免误操作其他开关；自动勾选“不再提示”、点击确认，并回读弹窗关闭结果。
- 将后续确认流程也改为统一状态探测，避免 AI 弹窗、额度错误、登录失效和订单状态互相误判。
- Playwright Sync API 的会话健康检查、登录、合成、下载全部固定到专用线程，避免 asyncio 事件循环与 greenlet 跨线程异常。
- 保持按精确 `worksId` 匹配作品、进入下载页批量勾选并统一下载，避免同名或最新作品错配。

### 降低生成和渲染过程的资源占用

- 进度文件保存改为后台线程节流写入，统计状态改为增量维护并限制事件推送频率，减少大文档生成时的磁盘 I/O、JSON 序列化和 O(n²) 扫描。
- 多角色音频拼接在音频参数一致时直接合并原始 PCM 数据，MP3 导出后只做轻量文件头校验，移除重复的 FFmpeg 回读和反复音频复制。
- 结果列表使用 `DocumentFragment` 批量挂载，波形占位条由 64 个减少为 32 个，启用 `content-visibility` 和布局隔离。
- 音频列表搜索采用 `requestAnimationFrame` 合帧；根据设备 CPU/内存启用低性能模式，关闭非必要装饰动画但保留生成进度反馈。
- 普通运行不再将后端每条 stdout 日志复制到 Electron 主进程控制台，同时仍持续消费管道避免阻塞；开发调试可通过 `--dev` 或 `WORDTTS_DEBUG_LOGS=1` 恢复日志转发。

### 精简桌面端打包体积

- Chromium 仅保留中文简体和英文语言资源。
- 移除不参与讯飞网页自动化的 Widevine 目录及 Playwright 的类型、协议和 Vite 开发期资源。
- 保留 Playwright driver、Chromium 和 Electron 运行所需文件，兼容 macOS 与 Windows 打包流程。

### 测试与回归保障

- 增加真实 Ant Design AI 标识/作品设置 DOM 回归测试。
- 增加页面首次探测为空、后续延迟挂载 AI 弹窗仍能被捕获的测试。
- 增加 MP3 快速校验、音频拼接、配置隔离和渲染器配置回归测试。

## v2.7.6

### 修复讯飞生成线程与 AI 标识确认

- 讯飞 Playwright Sync API 统一固定到专用单线程执行器，修复 Electron 任务中偶发的 `Cannot switch to a different thread`。
- AI 标识弹窗兼容真实 `.ant-modal-content` 结构；未成功勾选“不再提示”或确认、以及确认后弹窗未关闭时，任务会明确失败并记录弹窗状态。
- 收紧 AI 弹窗关键词，避免把仍在显示的“作品设置 / AI 标识”误判为 AI 说明弹窗。
- 修复本机 `npm run build:python` 在没有全局 `pyinstaller` 命令时无法构建的问题。

## v2.7.5

### 修复音色头像与讯飞会话线程问题

- 修复音色头像与首字母 fallback 并排渲染的问题；首屏头像直接加载，列表滚动时按可见区域加载其余头像。
- 修复已有讯飞 Playwright 会话在 asyncio 事件循环线程执行 Sync API 健康检查的问题，并消除首次并发创建重复浏览器会话的竞态。
- 针对讯飞 AI 标识说明弹窗的真实 Ant Design DOM 增加回归验证，确保勾选“不再提示”后点击“确认”。

## v2.7.4

### 修复音色选择与讯飞弹窗流程

- 音色列表改为按实际可用宽度自适应排列，避免被详情面板挤压；窄窗口自动切换为单列布局。
- 扩展讯飞 AI 标识弹窗识别，兼容不同 dialog、复选框和按钮结构，增加 Playwright locator 兜底并记录失败时的弹窗快照。
- 兼容部分讯飞页面首次确认后直接提交、不会再次显示“确认合成”的情况，避免误判失败后重复提交触发频控。

## v2.7.3

### 统一迁移至讯飞配音

- WordTTS 生成链路统一使用讯飞配音，移除 Edge TTS、TTSMaker 及其训练/回退代码。
- 女声固定为 Amanda，男声固定为 George；单词和例句不再使用独立音色，统一使用默认女声 Amanda。
- 语速、语调、音量统一为讯飞 0-100 整数参数，默认均为 50；前端提供滑块和数字输入框双向同步，后端也会强制校验范围。
- 讯飞返回的 MP3 不再执行首尾裁切、尾部补静音或应用层段落停顿拼接，保留原始音频内容。
- 默认男声、默认女声和文档角色改为独立保存三项参数，即使选择同一个音色也不会互相覆盖。
- 修复 AI 标识弹窗流程：自动勾选“不再提示”、确认并等待二次“确认合成”；文本改为一次性粘贴式输入。

### 兼容 Section A/B 课文跟读新格式

- 新格式通过 `Section`、跟读子题型和有效内容的结构组合判定，不再因单独出现 `Section A`、文件名或年级名而误切换；历史 `Understanding Idea/Reading for writing` 规则继续保留。
- 显式 `Conversation` 对话按角色边界拆分并把角色元数据交给音色配置；普通多角色段落保留为一个音频，多个 Conversation、角色名和 `SA/SB` 文件命名均保留。
- 文章按大标题、段落和“小标题 + 紧邻段落”生成音频，兼容 `//` 分割标记；结构化识别的 `Reading Plus` 使用 `RP` 命名前缀。

### 整理项目资源与运行路径

- 解析示例文档和结果统一移至 `examples/`，讯飞音色种子目录统一移至 `resources/`。
- 统一源码、Electron 开发版和 PyInstaller 版的资源/用户数据路径；音色缓存、音频输出和登录状态不再写入只读打包资源。
