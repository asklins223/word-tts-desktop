# 更新日志

## 2.4.0 - 2026-08-07

### 课文跟读段落/语篇逐句切分录音

- 段落跟读和语篇跟读改为逐句切分生成音频，每句话独立一个录音文件，不再整篇一个录音。
- 新增 `split_sentences()` 函数，按 `. ! ?` 句末标点切分英文句子，正确处理引号、括号等闭合符号。
- 段落跟读命名规则：`U-段落1`、`U-段落2` …（原为 `U-段落` 单一文件）。
- 语篇跟读命名规则：
  - 多篇语篇：`U-语篇1-1`、`U-语篇1-2`、`U-语篇2-1` …（语篇编号-句序号）。
  - 单篇语篇：`U-语篇1-1`、`U-语篇1-2` …（统一使用「语篇1」前缀，原为 `U-语篇` 无编号）。
  - Reading for writing 章节：`R-语篇1-1`、`R-语篇1-2` …（统一使用「语篇1」前缀，原为 `R-语篇` 无编号）。
- `PARSER_VERSION` 递增至 3，确保断点续传不复用旧解析结果。

## 2.3.0 - 2026-08-07

### 课文跟读语篇解析兼容与优化

- 修复新格式文档（如「课文跟读-7上U2」）标题层级与旧格式不同导致解析返回 0 条结果的问题：新格式中章节使用 Heading 1、子章节使用 Heading 2，解析器此前将子章节误认为章节标题。
- `_is_heading2` 重构为 `_is_section_heading`，同时支持 Heading 1 和 Heading 2 样式，并排除子章节名称（句子跟读/段落跟读/语篇跟读）的误匹配。
- 子章节标记检查提前到 Heading 检查之前，确保新格式中 Heading 2 样式的子章节被正确识别。
- 兼容 Understanding Idea 只有一篇语篇的情况：当语篇仅一篇时使用男声，文件命名移除数字后缀（`U-语篇` 而非 `U-语篇1`）；多篇时保持原有男/女声交替逻辑。
- 移除解析器中固定的语速（0.7）和停顿（400ms）常量 `TTS_RATE`/`TTS_PAUSE`，改为由前端动态配置统一控制，解析器只负责音色（voice）和文件名（filename_stem）的分配。
