# 更新日志

## 2.4.2 - 2026-08-19

### 修复「信息获取」文档每题行内参考答案导致只解析出一道题的问题

- 修复 `7上-U2-信息获取.docx` 等新格式文档只解析出一道题的问题：新格式中每道题后紧跟行内「参考答案：xxx」行，旧逻辑把任意包含「参考答案」的行当作整节结束标记，导致第一节第一题后采集立即终止。
- `RE_SECTION_END` 改为仅匹配独立成行的「参考答案」，行内「参考答案：xxx」不再中断采集（新增 `RE_INLINE_ANSWER` 直接跳过）。
- 「回答问题」题目可能漏编号（如 U2 的 7-9 题），新增兜底识别：英文问句（末尾 `?`）按说明行「回答第 7-10 个问题」中的题号区间顺延编号（新增 `RE_ANSWER_RANGE` / `RE_UNNUMBERED_QUESTION`）。
- `PARSER_VERSION` 递增至 5，`PARSE_CACHE_VERSION` 递增至 4，避免复用旧解析缓存/进度。

## 2.4.1 - 2026-08-12

### 修复 Understanding Idea 句子跟读缺失问题

- 修复 `课文跟读-G7-3.docx` 等文档中 Understanding Idea 章节的句子跟读未被解析的问题：该章节的句子没有编号前缀（如 `1.`、`2.`），导致 `_handle_sentence` 方法跳过了全部 9 条句子。
- `_handle_sentence` 新增对无编号格式的支持：当文本不匹配编号正则时，跳过中文翻译行和纯中文行后，自动编号并加入缓冲区。
- `PARSER_VERSION` 递增至 4，`PARSE_CACHE_VERSION` 递增至 3，确保旧缓存/进度不复用。

## 2.4.0 - 2026-08-07

### 课文跟读段落/语篇逐句切分录音

- 段落跟读和语篇跟读改为逐句切分生成音频，每句话独立一个录音文件，不再整篇一个录音。
- 新增 `split_sentences()` 函数，使用状态机跟踪引号开闭状态，确保引号内部的 `. ! ?` 不会触发切分；闭合引号后跟大写字母且引号内以句末标点结尾时才切分（逗号结尾的对话标签如 `"Hello," she said.` 不切分）。支持直引号和智能引号。
- 段落跟读命名规则：`U-段落1`、`U-段落2` …（原为 `U-段落` 单一文件）。
- 语篇跟读命名规则：
  - 多篇语篇：`U-语篇1-1`、`U-语篇1-2`、`U-语篇2-1` …（语篇编号-句序号）。
  - 单篇语篇：`U-语篇1-1`、`U-语篇1-2` …（统一使用「语篇1」前缀，原为 `U-语篇` 无编号）。
  - Reading for writing 章节：`R-语篇1-1`、`R-语篇1-2` …（统一使用「语篇1」前缀，原为 `R-语篇` 无编号）。
- `PARSER_VERSION` 递增至 3，确保断点续传不复用旧解析结果。
- `PARSE_CACHE_VERSION` 递增至 2，`server.py` 不再复用旧版解析缓存（缺少 `filename_stem`/`voice` 字段）。
- `server.py` 的 `progress_is_reusable` 增加 `parser_version` 校验，config 增加 `parser_version` 字段，防止旧进度被误复用。

## 2.3.0 - 2026-08-07

### 课文跟读语篇解析兼容与优化

- 修复新格式文档（如「课文跟读-7上U2」）标题层级与旧格式不同导致解析返回 0 条结果的问题：新格式中章节使用 Heading 1、子章节使用 Heading 2，解析器此前将子章节误认为章节标题。
- `_is_heading2` 重构为 `_is_section_heading`，同时支持 Heading 1 和 Heading 2 样式，并排除子章节名称（句子跟读/段落跟读/语篇跟读）的误匹配。
- 子章节标记检查提前到 Heading 检查之前，确保新格式中 Heading 2 样式的子章节被正确识别。
- 兼容 Understanding Idea 只有一篇语篇的情况：当语篇仅一篇时使用男声，文件命名移除数字后缀（`U-语篇` 而非 `U-语篇1`）；多篇时保持原有男/女声交替逻辑。
- 移除解析器中固定的语速（0.7）和停顿（400ms）常量 `TTS_RATE`/`TTS_PAUSE`，改为由前端动态配置统一控制，解析器只负责音色（voice）和文件名（filename_stem）的分配。
