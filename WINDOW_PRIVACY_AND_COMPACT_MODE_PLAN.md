# 小猪自动化窗口控制与小窗完整运行方案

## 1. 文档目的与实施范围

本文定义小猪应用的窗口行为、任务控制、自动化浏览器控制、小窗工作台、设置和验收标准，作为实现与验收依据。

本文同时记录当前代码基线的实现状态。功能代码已经按第 12.1 节落地并完成自动化回归；Windows/macOS 的原生窗口行为、权限和多显示器表现仍需在对应真机上完成发布前验收，不能由单元测试替代。

当前桌面架构为 Electron 主进程/渲染层 + Python FastAPI 后端 + Python Playwright 自动化浏览器。本方案第一阶段以 macOS 和 Windows 桌面安装包为正式验收范围；Linux 可继续保留开发运行能力，但不纳入本阶段窗口和平台行为承诺。

第一阶段的代码实现已经覆盖“专用浏览器窗口隐藏/恢复”“真实任务暂停/继续”和完整小窗工作台；当前进入平台联调与发布验收阶段。若某个平台无法满足窗口级隐藏，应降低该平台的产品承诺，而不是用未验证的实现直接进入发布。

## 2. 已确认的需求

### 2.1 一键隐藏与防偷窥模式

应用需要区分两个概念：

- **隐藏应用窗口**：只隐藏小猪主窗口；自动化浏览器是否隐藏由窗口策略决定。
- **防偷窥模式**：隐藏小猪主窗口，同时隐藏专用自动化浏览器。这是面向防偷窥场景的完整行为，默认必须隐藏浏览器。

用户可以通过按钮或全局快捷键进入防偷窥模式，目标行为如下：

- 小猪主窗口不可见；
- 专用自动化浏览器的所有相关窗口和弹窗不可见；
- 不创建系统托盘或菜单栏图标；
- Windows 任务栏、macOS Dock 和应用切换器的行为按平台能力明确验收；
- 是否在进入防偷窥模式时自动暂停任务，由设置决定；
- 隐藏操作不结束任务，不清除登录状态；
- 应用进程仍保持运行，恢复后可以继续查看任务状态。

“只隐藏小猪、不隐藏浏览器”不能称为防偷窥模式，并且不能作为防偷窥模式的默认行为。

没有托盘图标时，应用必须始终保留至少一个可用的恢复入口：默认全局恢复快捷键不能被完全禁用；若用户要修改它，必须先配置另一个有效的恢复快捷键。快捷键失效或注册失败时，应在设置中心明确提示。

这里的目标是控制小猪窗口和专用浏览器的可见性，不是隐藏整个系统任务栏或 macOS Dock。应用还应使用单实例唤醒机制：用户在应用已隐藏时再次启动应用，已有实例必须接收唤醒请求、恢复小猪窗口并聚焦，而不是再启动第二个后端或第二个任务。命令行 `--show` 可以作为开发和故障排查入口，但不能替代面向普通用户的恢复快捷键和单实例唤醒。

防偷窥不承诺阻止任务管理器、活动监视器、进程列表、录屏软件或系统级屏幕共享发现程序。

### 2.2 小窗运行模式

小窗模式不是单纯的进度展示窗口，而是一个可以完成完整业务流程的紧凑工作台。

用户应当能够在小窗内完成：

- 选择或拖拽文件；
- 查看解析结果；
- 确认和修改内容；
- 设置配音参数；
- 开始生成；
- 暂停、继续或终止任务；
- 查看错误并重试；
- 播放和预览最终效果；
- 下载文件或打开输出目录。

除首次登录或人工验证外，不要求用户切换到完整窗口才能完成流程。

小窗优先复用同一个 Electron `BrowserWindow`，通过窗口模式和布局状态切换，不重复创建一套独立的业务页面，避免两个渲染器之间出现任务状态和表单状态不一致。

建议默认尺寸为 `520 × 680`，允许用户调整大小。`420 × 520` 只能作为需要专项验证的极限目标，不能在未经高 DPI 和长文本验证前作为强制可用标准。

### 2.3 独立暂停 / 继续功能

小窗和完整窗口都提供独立的任务控制按钮，后端任务控制器是唯一状态来源。

- 运行中显示“暂停任务”；
- 暂停请求处理中显示“正在暂停”；
- 暂停确认后显示“继续任务”；
- 恢复请求处理中显示“正在恢复”；
- 暂停不关闭 Chrome；
- 暂停不关闭当前页面；
- 暂停不清除登录状态；
- 继续时复用原来的 Chrome 页面和会话；
- 另提供“终止任务”，用于明确结束任务并释放浏览器资源；
- 终止、暂停和开始新任务不能共用含义不清的 cleanup 操作。

暂停是协作式暂停，只能在安全检查点生效。正在执行的底层 Playwright 调用可能需要先结束，界面必须显示“正在暂停”，不能在收到请求的瞬间虚报为“已暂停”。

### 2.4 设置中心

新增设置界面，允许用户配置：

- 防偷窥模式是否自动暂停任务；
- 恢复窗口后是否自动继续任务；
- 隐藏、显示、暂停、继续和终止的快捷键；
- 小窗尺寸和默认运行模式；
- 浏览器窗口的隐藏行为；
- 重试、超时、通知、日志等常用配置。

影响主进程的窗口、快捷键和恢复设置由 Electron 主进程持久化；任务控制和生成参数由后端任务会话保存。桌面设置采用单一所有者：由 Electron 主进程负责读写用户数据目录下的版本化 `settings.json`，渲染层只能通过受信任 IPC 读取和修改；不同时维护 Electron 和 Python 两套桌面设置的双向同步。后端只接收带 schema 版本的任务配置快照，并负责任务配置的校验、迁移和持久化。设置文件需要采用原子写入，避免应用异常退出造成半写入配置。

## 3. 总体设计原则

### 3.1 不使用 Chrome 无头模式

Chrome 始终使用有界面模式运行，以优先保证登录流程、页面兼容性和自动化稳定性。

防偷窥通过操作系统窗口能力实现：

- 普通最小化：窗口进入任务栏或 Dock，兼容性较好，但可能留下图标；
- 后台隐藏：窗口不显示在屏幕前方，适用于防偷窥要求；
- 如果要求任务栏/Dock 完全没有图标，不能只做普通最小化，必须使用后台隐藏以及平台相关的图标处理。

“无图标”只针对小猪或专用自动化浏览器的相关窗口/应用，不代表可以隐藏或修改整个系统任务栏、Dock 或其他应用的显示。

### 3.2 应用窗口和自动化浏览器分开管理

小猪窗口的显示状态不等于 Chrome 的显示状态，两者由不同控制器管理：

- Electron 主进程管理小猪窗口的完整、小窗和隐藏状态；
- 自动化浏览器控制器管理专用 Chrome 的显示、隐藏和人工验证显示；
- 后端任务控制器管理生成任务的运行、暂停、继续和终止；
- 前端只展示后端和主进程确认过的状态，不自行推断“已暂停”或“浏览器已隐藏”。

### 3.3 自动化 Chrome 使用独立配置和可追踪身份

自动化浏览器使用独立配置目录、独立进程，并保存可追踪的浏览器身份信息，避免误操作用户自己的 Chrome 窗口、标签页和登录状态。

浏览器身份至少包括：

- 启动进程 PID；
- 实际可执行文件路径；
- 配置目录路径；
- 当前 Playwright context 和所有 page；
- Windows 窗口句柄或 macOS 对应的应用/窗口标识；
- 控制器创建时间和当前控制状态。

不能只通过窗口标题识别 Chrome，也不能向用户正在使用的普通 Chrome 发送隐藏、显示或关闭命令。

发布版的自动化浏览器默认使用随应用打包的 Playwright Chromium 或独立 Chromium 副本。`PLAYWRIGHT_BROWSERS_PATH` 只负责定位浏览器资源目录，不能替代实际可执行文件选择；启动时应通过 Playwright 的平台解析结果取得真实 executable path，不能硬编码 `chromium-xxx/chrome` 这类路径。系统 Chrome 只能作为明确的调试/兼容性降级选项，启用后必须降低 macOS Dock/应用级图标相关承诺，并继续使用独立 profile。

### 3.4 后端任务状态是唯一事实来源

窗口状态、浏览器状态和任务状态必须分开建模。建议使用以下三组状态：

```text
AppWindowMode: full | compact | hidden
BrowserVisibility: visible | hidden | minimized | manual_required | unavailable
TaskControl: idle | starting | running | pause_requested | paused
             | resume_requested | terminating | terminated | completed | failed
```

其中 `pause_requested`、`paused`、`resume_requested` 和 `terminating` 都是可见状态，便于处理底层调用尚未结束的短暂阶段。

## 4. 三种运行模式

| 模式 | 小猪窗口 | 自动化 Chrome | 任务状态 |
| --- | --- | --- | --- |
| 普通模式 | 完整窗口 | 按浏览器策略显示或隐藏 | 正常运行、可暂停 |
| 小窗模式 | 紧凑但可操作的完整工作台 | 默认后台隐藏或最小化 | 正常运行、可暂停 |
| 防偷窥模式 | 完全隐藏 | 必须后台隐藏；不能只最小化 | 按设置运行或暂停 |

### 4.1 普通模式

用于首次登录、人工验证、详细编辑和问题排查。所有完整功能保持现有使用方式。

### 4.2 小窗模式

小窗是完整流程的紧凑版本，不是只读状态面板。

默认尺寸为 `520 × 680`，允许用户调整大小；`420 × 520` 只能作为需要专项验证的极限目标，不能在未经高 DPI 和长文本验证前作为强制可用标准。

建议布局：

- 顶部：应用名称、当前状态、隐藏按钮、设置按钮；
- 中部：当前步骤和可滚动内容；
- 底部：固定操作栏，例如下一步、开始生成、暂停、继续、下载；
- 侧边导航在小窗中折叠为步骤指示器或隐藏；
- 内容过长时：内部滚动，不强制切换完整窗口；
- 高级参数：使用可展开面板，仍然在小窗内完成配置；
- 最小可用尺寸必须在 100%、125% 和 150% 显示缩放下分别验证。

### 4.3 防偷窥模式

小猪主窗口和专用自动化 Chrome 都不可见。用户通过自定义全局快捷键恢复窗口，或者在需要时使用系统级恢复入口。

如果隐藏时设置为自动暂停，恢复后默认保持暂停，用户点击“继续任务”后再恢复运行，以避免误操作。

防偷窥模式不因隐藏操作而关闭会话或删除输出文件。

## 5. 小窗模式的完整业务流程

### 5.1 选择文件

小窗内提供：

- 选择文件按钮；
- 拖拽文件区域；
- 最近使用文件；
- 文件格式、大小和读取状态提示；
- 重新选择和移除文件。

拖拽文件由渲染层接收并校验，只有原生文件选择对话框需要通过 IPC 调用主进程；不为已有的拖拽流程额外复制一套文件 IPC 或文件流通道。

### 5.2 解析和内容确认

文件选择后进入解析阶段，显示：

- 解析进度；
- 文本或条目数量；
- 内容摘要；
- 可滚动的内容预览；
- 重新解析；
- 返回重新选择文件。

解析结果需要区分原始解析结果和用户编辑后的任务输入：

- 原始文件保持不变；
- 用户可以编辑待生成文本、条目顺序、角色或允许编辑的结构字段；
- 修改后的结果保存到当前 session，并显示“已修改”标记；
- 生成前重新校验字段、数量、文件名和角色配置；
- 内容发生变化后，旧的解析缓存和生成进度不能直接复用，除非通过内容版本和配置版本重新确认可复用。

### 5.3 配音参数配置

小窗内直接提供常用配置：

- 音色；
- 语速；
- 音量；
- 音调；
- 输出格式；
- 保存位置；
- 自动合并等相关选项。

高级配置使用折叠区域，不能因为使用小窗就丢失功能。配置修改后应明确影响范围：仅影响下一次生成，还是使当前断点进度失效。

### 5.4 开始生成

开始后显示：

- 当前处理条目；
- 总进度；
- 当前阶段；
- 暂停任务；
- 继续任务；
- 终止任务；
- 错误详情和重试入口。

自动化 Chrome 保持后台隐藏或最小化，不影响任务执行。

### 5.5 最终效果预览

生成完成后，小窗内提供：

- 播放最终音频；
- 播放、暂停、重新播放；
- 预览单个片段；
- 预览完整结果；
- 重新生成；
- 下载文件；
- 打开输出目录；
- 返回修改参数。

预览直接使用应用内的本地播放器，不依赖 Chrome 窗口。

### 5.6 异常处理

以下情况均在小窗内处理：

- 文件解析失败；
- 登录失效；
- Chrome 启动失败；
- 页面操作超时；
- 网络错误；
- 生成失败；
- 任务暂停或暂停超时；
- 任务终止；
- SSE 断线和状态恢复。

需要人工登录或验证时，提供“显示自动化浏览器”按钮。显示前应暂停或阻止新的自动化操作；完成后由用户明确点击隐藏，或按浏览器设置再次隐藏。

## 6. 暂停、继续和终止逻辑

### 6.1 任务状态

建议状态流转如下：

```text
待机 → 启动中 → 运行中 → 正在暂停 → 已暂停
                         ↑          ↓
                         └ 正在恢复 ┘

运行中 / 已暂停 / 正在恢复 → 正在终止 → 已终止
运行中 → 已完成
运行中 → 已失败
```

后端任务控制器必须校验状态流转，重复点击同一个请求应返回当前状态，而不是创建第二个控制动作。暂停、继续和终止接口都需要具备幂等性。

当前应用使用全局专用 Playwright 线程和有界浏览器会话，第一阶段明确只允许一个生成任务占用自动化浏览器。暂停一个任务时，不允许另一个任务抢占同一页面；若未来需要任务排队，应另行定义队列状态。

### 6.2 暂停任务

暂停请求发出后：

- 后端将状态置为 `pause_requested`；
- 脚本不再开始新的自动化步骤；
- 在当前安全操作完成后进入 `paused`；
- 已提交但尚未下载的作品不因暂停而重复提交；
- 重试、退避等待、下载、整理和下一条任务都必须检查暂停信号；
- 暂停期间保留 Chrome 进程、浏览器上下文、页面、cookies 和任务索引；
- 不继续生成、重试或下载；
- 继续显示任务状态和最近检查点。

安全检查点至少包括：

- 页面跳转前后；
- 点击和输入操作前后；
- 等待生成结果时的轮询间隔；
- 作品提交前后；
- 下载前后；
- 重试前后；
- 每个条目或合并作品完成后。

暂停不能强制冻结正在执行的底层浏览器调用。调用的超时必须有上限，界面应显示“正在暂停”，直到后端发出明确的 `paused` 状态。

暂停控制必须有跨 asyncio 事件循环和同步 Playwright 线程的线程安全桥接：

- 后端维护一个带锁的任务控制对象，至少包含 `pause_requested`、`resume_requested`、`terminate_requested` 和当前检查点；
- asyncio 层可以使用 `asyncio.Event` 负责 API/任务协程的唤醒，但同步 Playwright 线程不能直接等待或操作 asyncio 的 Event，必须读取 `threading.Event`、线程安全状态或等价桥接对象；
- `_poll`、作品 ID 消费、编辑器选区、提交、下载、退避和重试等同步路径统一接收控制探针，并把长等待拆成有上限的可检查等待；
- 只有同步调用返回并到达检查点后，后端才能把 `pause_requested` 更新为 `paused`，不能只因收到 HTTP 请求就宣称暂停成功。

### 6.3 继续任务

继续请求将状态置为 `resume_requested`，清除暂停门闩，并复用原页面和上下文，从最近安全检查点继续。

继续前必须检查：

- 页面是否仍然存在且未关闭；
- 登录是否仍然有效；
- 当前页面是否处于预期状态；
- 浏览器进程和窗口身份是否仍属于本次自动化会话；
- 输出文件、作品 ID、任务索引和内容版本是否一致。

如果页面状态已经变化，显示明确错误并提供重新登录、重新显示浏览器或重新执行当前安全步骤的入口。不能盲目重放可能已经计费的提交操作。

### 6.4 终止任务

终止是和暂停不同的明确结束操作：

- 状态先进入 `terminating`；
- 停止后续生成、等待、重试和下载；
- 等待当前可安全结束的浏览器调用退出；
- 关闭本次自动化页面、浏览器上下文和浏览器进程；
- 保留已经生成的音频、作品对账信息和任务日志；
- 将任务标记为 `terminated`。

终止不能直接复用“开始新任务”的清理逻辑。开始新任务可以在用户确认后清理未归档临时文件；终止任务必须先保留已有输出，必要时将当前 session 转为可恢复的终止记录。

任务完成或失败后的 Chrome 是否关闭由 `close_browser_on_finish` 设置决定，默认关闭。无论该设置如何，显式终止都必须关闭本次浏览器会话；暂停期间不能关闭浏览器。

## 7. 一键隐藏与暂停的组合行为

### 7.1 防偷窥模式组合规则

| 当前任务状态 | 自动暂停开启 | 自动暂停关闭 |
| --- | --- | --- |
| 未运行 | 隐藏小猪和专用浏览器 | 隐藏小猪和专用浏览器 |
| 运行中 | 隐藏浏览器并请求暂停，再隐藏小猪 | 隐藏小猪和专用浏览器，任务继续运行 |
| 正在暂停 | 隐藏小猪和专用浏览器，等待暂停完成 | 隐藏小猪和专用浏览器，保持当前控制状态 |
| 已暂停 | 隐藏小猪和专用浏览器，保持暂停 | 隐藏小猪和专用浏览器，保持暂停 |
| 已完成/失败/已终止 | 隐藏小猪和专用浏览器 | 隐藏小猪和专用浏览器 |

如果用户只触发“隐藏应用窗口”而不是防偷窥模式，则可按浏览器策略决定是否隐藏 Chrome，但界面必须明确告诉用户当前不是防偷窥状态。

### 7.2 恢复规则

恢复窗口后默认不自动继续任务。可以通过 `恢复窗口后自动继续任务` 开启自动继续，但默认关闭。

无论任务是否暂停，恢复窗口都不应自动显示 Chrome。只有用户明确点击“显示自动化浏览器”，或设置明确允许自动显示时，才可以显示浏览器。

恢复过程必须从后端重新读取任务控制状态，不能只依赖渲染层上一次内存状态。若渲染器重载或 SSE 断线，恢复后应先通过 session 状态和事件快照重建界面。

## 8. 设置中心设计

### 8.1 窗口与防偷窥

- 默认启动模式：普通 / 小窗；防偷窥启动模式需要用户明确开启；
- 防偷窥模式是否自动暂停任务；
- 恢复窗口后是否自动继续；
- 小窗默认宽度和高度；
- 隐藏应用窗口和进入防偷窥模式使用不同的文字与状态提示；
- 继续任务后是否保持 Chrome 隐藏；
- 任务完成后是否自动恢复窗口；
- 任务失败后是否自动恢复窗口。

防偷窥模式中专用浏览器必须隐藏；“是否隐藏 Chrome”只影响普通模式和“隐藏应用窗口”动作，不能覆盖防偷窥模式的安全默认值。

桌面设置由 Electron 主进程单独负责读写 `<userData>/settings.json`。渲染层的旧版 `localStorage` 配置只允许在升级时做一次性迁移；后端不直接写入桌面设置，也不与 Electron 形成双向同步。任务启动时把重试、超时、日志、输出和生成参数连同配置版本写入 session 快照。

### 8.2 快捷键

支持自定义：

- 防偷窥模式 / 恢复窗口；
- 暂停 / 继续当前任务；
- 终止当前任务；
- 切换小窗 / 完整窗口。

快捷键设置应支持：

- 应用内部冲突检测；
- 注册成功/失败提示；
- 禁用非关键快捷键；
- 恢复默认快捷键；
- 修改后立即生效；
- 始终保留至少一个有效的恢复窗口入口。

若主进程注册快捷键失败，设置页必须阻止保存“无恢复快捷键”的状态；应用隐藏后再次启动也必须走单实例唤醒并恢复窗口。

系统通常只能判断某个快捷键是否注册成功，不能保证提供冲突程序名称，因此不能承诺完整枚举系统冲突来源。

### 8.3 自动化任务

- 失败重试次数；
- 操作超时时间；
- 是否保留任务日志；
- 是否显示完成通知；
- 是否自动打开输出目录；
- 任务结束后是否关闭自动化 Chrome；
- 是否保留最近任务记录及保留数量。

任务控制、输出保留和历史记录删除必须分开。终止任务不得因为调用清理接口而删除已经生成的文件。

### 8.4 浏览器

- 自动化浏览器默认显示或后台隐藏；
- 首次登录时自动显示浏览器；
- 登录完成后自动隐藏浏览器；
- 是否允许调试模式显示 Chrome；
- 自动化浏览器配置目录管理。

第一阶段不允许用户在任务运行期间随意切换配置目录，也不开放未经校验的任意路径写入。

### 8.5 内容编辑与生成输入

- 显示原始解析结果和当前任务输入版本；
- 内容修改后立即标记未保存或已修改；
- 生成前保存并校验修改；
- 内容版本变化时使不匹配的旧进度失效；
- 错误重试沿用当前已确认的内容版本。

### 8.6 推荐默认值

- 防偷窥模式自动暂停：开启；
- 恢复窗口后自动继续：关闭；
- 继续任务后保持 Chrome 隐藏：开启；
- 任务完成后自动恢复窗口：关闭；
- 首次登录时显示 Chrome：开启；
- 任务失败时恢复窗口：开启；
- 完成/失败后关闭 Chrome：开启；
- 防偷窥模式隐藏专用浏览器：强制开启。

## 9. 技术模块划分

### 9.1 Electron 主进程

负责：

- 完整窗口、小窗和隐藏状态；
- 保存和恢复窗口尺寸、位置和当前模式；
- Windows 任务栏和 macOS Dock 的显示控制；
- 全局快捷键注册、注销和失败处理；
- 主进程设置存储；
- 通过受信任 IPC 与渲染层同步窗口状态；
- 将“显示/隐藏自动化浏览器”的请求转交浏览器控制器，不根据标题自行操作用户 Chrome。

主进程与渲染层的最小 IPC 契约已经落地：`window:get-state`、`window:set-mode`、`window:hide`、`window:show`，`settings:get/update/reset/import-legacy`，`task:pause/resume/terminate`，以及 `browser:get-state/show/hide`。任务暂停、继续和终止仍统一使用 9.3 的后端接口；IPC 只负责受信任转发，不另实现一套任务状态机。

窗口应先完成加载并建立控制器，再执行隐藏。不能把“启动时创建隐藏 BrowserWindow”作为默认方案；Windows 环境已经出现过隐藏窗口创建导致渲染器异常的风险，需要用加载后隐藏并进行回归测试。

主进程应启用单实例锁。收到第二次启动请求时，已有实例负责恢复并聚焦窗口；不得因为第二次启动而创建第二个后端、浏览器会话或任务。

### 9.2 前端渲染层

负责：

- 小窗完整流程界面；
- 完整窗口界面；
- 设置中心；
- 暂停、继续、终止按钮；
- 文件选择、内容编辑、参数配置和效果预览；
- 展示后端确认的任务、窗口和浏览器状态；
- SSE 断线后的状态重建。

渲染层不能通过本地布尔变量直接把任务标为“已暂停”。当前实现通过 session 状态和 SSE 控制事件重建状态，并把内容编辑、设置中心、任务控制和浏览器状态接入完整/小窗两种布局。

### 9.3 后端任务控制器

负责：

- 保存当前任务上下文；
- 接收暂停、继续和终止请求；
- 在安全检查点响应暂停信号；
- 向前端推送任务状态、控制状态、阶段、检查点和错误；
- 区分暂停、终止、完成、失败和新任务清理；
- 保证控制请求幂等并校验非法状态流转；
- 处理 SSE 重连后的状态快照。

已落地的最小接口契约：

```text
GET  /api/session/{session_id}
POST /api/session/{session_id}/pause
POST /api/session/{session_id}/resume
POST /api/session/{session_id}/terminate
PUT  /api/session/{session_id}/content
POST /api/session/{session_id}/browser/show
POST /api/session/{session_id}/browser/hide
```

`GET /api/session/{session_id}` 至少返回 `task_control`、`phase`、`last_checkpoint`、`content_version`、`browser_state`、已生成文件摘要和错误信息。

SSE 事件需要增加或明确以下类型/字段：

- `control_state`：`pause_requested`、`paused`、`resume_requested`、`terminating` 等；
- `checkpoint`：最近安全检查点和可恢复说明；
- `browser_state`：浏览器显示、人工验证和可用性；
- `stats`：保留现有进度字段；
- `done`、`error`、`cancelled` 或 `terminated`：明确终态并可重放。

### 9.4 自动化浏览器控制器

负责：

- 启动和跟踪专用 Chrome 进程；
- 发布版优先解析并启动随应用打包的 Playwright Chromium；系统 Chrome 只能由明确的调试/兼容性设置启用；
- 保存进程、配置目录、context、page 和窗口身份；
- 跟踪所有 page 和弹窗；
- 对指定窗口进行显示、最小化和后台隐藏；
- 在人工登录/验证时显示浏览器；
- 暂停时保留页面和上下文；
- 终止或按设置完成释放浏览器资源。

当前 Chrome 由 Python Playwright 启动，因此浏览器控制器应位于 Python 后端或由后端调用的平台原生辅助模块中，不能假设 Electron 可以直接获得 Chrome 的 `BrowserWindow`。Electron 只负责小猪自身窗口和控制请求转发。

浏览器控制器应在启动后记录 Playwright 实际解析出的 executable path、PID/进程树、profile、context、page 和平台窗口身份。`PLAYWRIGHT_BROWSERS_PATH` 只负责定位浏览器资源目录，不能覆盖显式传入的系统 Chrome 路径；应通过 Playwright 的平台解析结果取得真实 executable path，不能硬编码 `chromium-xxx/chrome` 这类路径。Windows 可通过 Win32 窗口句柄控制专用窗口；macOS 可通过平台原生窗口 API，并在需要时请求辅助功能权限。`node-ffi`、`active-win` 或其他具体库不是前置约束，关键是身份绑定、权限提示和不误伤用户 Chrome。

### 9.5 设置存储

Electron 主进程是桌面设置的唯一读写者，保存到用户数据目录下的 `settings.json`，配置包含 schema 版本，采用原子写入并在启动时校验。渲染层通过 IPC 获取和更新设置；Python 后端不直接写桌面设置，只接收带版本的任务配置快照，并在任务 session 中持久化。升级时可从旧版渲染层 `localStorage` 做一次性迁移，但不得长期保留两套互相覆盖的设置源。

### 9.6 内容编辑与生成输入

负责：

- 保存规范化的可编辑解析结果；
- 校验条目、角色、文本和文件名；
- 生成内容版本指纹；
- 在内容变化后使不匹配的缓存/进度失效；
- 让生成接口使用当前 session 中已确认的内容，而不是无条件重新读取原始文件。

## 10. Windows 和 macOS 边界

### 10.1 Windows

- 小猪窗口可以隐藏任务栏图标，但必须验证 Alt+Tab、任务栏恢复和快捷键恢复行为；
- 自动化 Chrome 通过启动进程和窗口句柄控制指定窗口；
- 普通最小化会留下任务栏图标；
- 防偷窥模式使用后台隐藏，不能只使用普通最小化；
- 窗口隐藏必须覆盖专用浏览器的所有相关窗口，不得按标题误伤用户 Chrome；
- 在 100%、125% 和 150% 缩放下验证小窗布局；
- 验证窗口加载后隐藏，不把隐藏创建作为启动依赖。

这里的“隐藏任务栏图标”只指小猪窗口本身，不是隐藏整个 Windows 任务栏。应用已隐藏时再次启动客户端，应通过单实例唤醒恢复已有窗口。

### 10.2 macOS

- 可以隐藏专用自动化浏览器窗口，但可能需要辅助功能权限；
- `app.dock.hide()` 只影响小猪应用，系统 Chrome 的 Dock 图标属于 Chrome 应用本身；
- 当前若使用系统 Google Chrome，隐藏单个窗口不一定能移除 Chrome Dock 图标；
- 防偷窥模式已使用随应用打包的 Playwright Chromium；是否能让 Dock 完全没有自动化浏览器图标仍需在真实 macOS 有界面环境验证，不能仅凭开发机自动化测试承诺；
- 当前代码默认使用随应用打包的 Playwright Chromium，并通过 Playwright 实际解析出的 executable path 启动；不能只设置 `PLAYWRIGHT_BROWSERS_PATH` 后继续传入系统 Chrome 的显式 `executable_path`；
- 系统 Chrome 仅允许作为明确的调试/兼容性降级路径；一旦使用，不能承诺自动化浏览器的 macOS Dock 应用级图标消失；
- 不创建菜单栏托盘图标；
- 用户拒绝辅助功能权限时，必须明确提示“只能隐藏小猪窗口/不能完成完整防偷窥”，不能静默宣称成功；
- 验证多显示器、Mission Control、Cmd+Tab 和快捷键恢复行为。

### 10.3 共同边界

- 第一阶段只承诺 macOS 和 Windows；
- 不影响用户自己的 Chrome 是硬性验收项；
- 不能保证从系统进程列表、任务管理器或录屏软件中消失；
- 使用系统 Chrome 时，Dock/任务栏应用级图标是否消失必须单独列为平台能力，而不是统一承诺。

## 11. 验收标准

### 11.1 防偷窥

- 点击防偷窥后，小猪窗口不可见；
- 专用自动化 Chrome 的所有相关窗口不可见；
- 用户自己的 Chrome 不被隐藏、最小化或关闭；
- 任务按设置选择暂停或继续；
- 快捷键可以恢复小猪窗口；
- 至少保留一个有效的恢复入口；
- 应用隐藏后再次启动客户端可以唤醒并恢复已有实例，不会创建第二个后端或任务；
- 恢复后 Chrome 不会未经用户允许自动弹出；
- macOS 权限不足时有明确提示；
- Windows/macOS 的任务栏、Dock、Alt+Tab、Cmd+Tab 和多显示器行为符合对应平台说明。

### 11.2 小窗完整流程

- 在小窗状态可以选择文件和拖拽文件；
- 可以完成解析、预览和内容编辑；
- 修改后的内容可以保存并用于生成；
- 内容变化会正确处理旧缓存和旧进度；
- 可以修改常用参数并启动生成；
- 可以暂停、继续和终止任务；
- 可以查看错误、重试和重新登录；
- 可以播放最终效果、下载文件和打开输出目录；
- 在目标最小尺寸、高 DPI、长文件名和长错误信息下仍可操作。

### 11.3 暂停和继续

- 暂停先显示“正在暂停”，收到后端确认后才显示“已暂停”；
- 暂停不会关闭 Chrome、页面、context 或登录状态；
- 暂停后不再产生新的提交、重试、下载或整理操作；
- 在每个安全检查点都能响应暂停；
- 继续前会检查页面、登录状态、作品 ID、输出文件和内容版本；
- 继续后复用原浏览器会话；
- SSE 断线或渲染器重载后仍能恢复真实控制状态；
- 终止会关闭浏览器，但不删除已经生成的文件；
- 新建任务的清理不会误删已终止任务的保留输出；
- 重复暂停、继续、终止请求不会创建重复任务或重复提交。

### 11.4 设置

- 防偷窥时自动暂停配置有效；
- 自定义快捷键可以保存并生效；
- 注册失败或应用内部冲突有明确提示；
- 永远保留有效恢复快捷键；
- 重启应用后窗口、快捷键和策略设置仍然保留；
- 恢复默认设置后行为回到默认值；
- 配置文件损坏时可以安全回退默认值而不影响历史音频；
- 桌面设置只有一个权威存储源，旧版配置迁移不会与新设置互相覆盖。

### 11.5 崩溃和重启

- 应用正常隐藏/恢复不会结束后端任务；
- 应用或系统崩溃后，已保存检查点和已生成文件可以被识别；
- 不承诺从任意底层浏览器操作中间位置精确恢复；
- 计费提交状态不确定时不能盲目重复提交，应进入对账或人工确认流程。

## 12. 实施顺序

1. Windows/macOS 浏览器与窗口控制 POC：合并验证打包 Chromium 选择、专用浏览器身份、所有窗口隐藏/恢复、用户 Chrome 隔离、系统图标、权限、快捷键和单实例唤醒；
2. 后端任务控制内核：先实现暂停、继续、终止、线程安全桥接、安全检查点和幂等状态流转，不依赖前端按钮；
3. 后端 API 与 SSE：实现控制接口、状态快照、检查点、断线重连和终态重放；
4. Electron 主进程与渲染层接入：实现窗口模式、设置单一存储、快捷键恢复、任务控制按钮和真实状态重建；
5. 内容编辑数据链路：实现编辑后的 session 内容、版本指纹、校验和进度失效规则；
6. 前端小窗完整工作台：完成文件、解析、内容、参数、生成、暂停、错误、预览和下载闭环；
7. 浏览器人工验证流程：完成显示浏览器、登录、验证、再次隐藏和状态恢复；
8. Windows/macOS 回归测试：任务栏、Dock、权限、快捷键、单实例唤醒、登录、暂停恢复、终止保留文件、高 DPI 和多显示器；
9. 最后进行 PyInstaller、Electron 打包、安装、升级和发布验证。

### 12.1 实施状态与验证结果

代码实现已经完成，以下能力已进入当前基线：

- Electron 主进程使用单实例锁；应用隐藏后再次启动会唤醒已有窗口并聚焦，不创建第二个后端实例；
- 主进程提供窗口模式、隐藏/显示、设置、快捷键、任务控制和浏览器控制 IPC；始终保留全局恢复快捷键兜底，并在注册失败时给出恢复提示；
- 渲染层已接入完整/小窗工作台、文件拖拽、解析预览、内容编辑与 `content_version`、参数配置、暂停/继续/终止、错误重试、历史结果、播放、下载和输出目录；
- 自动化浏览器默认使用随应用打包的 Playwright Chromium，并记录 executable path、PID、profile、context、page 和平台窗口身份；系统 Chrome 只能通过 `WORDTTS_AUTOMATION_BROWSER=system` 显式启用；
- `SessionState` 已增加线程安全的 `pause_requested -> paused -> resume_requested` 控制桥，以及 `terminate -> terminated` 终态；同步 Playwright 线程使用 `threading.Event`，不会直接等待 asyncio 事件；防偷窥退出时也会恢复进入前的浏览器可见/最小化状态；
- 后端已提供 `/api/session/{id}/pause`、`/resume`、`/terminate`、`content` 和浏览器控制接口；SSE 支持控制状态、检查点、浏览器状态、终止事件和断线重放；
- 轮询、worksId 对账、多人编辑器选区、下载就绪等待和频控退避都切分为有上限的可检查等待；已确认提交的作品不会因为暂停或普通取消而重新提交；
- 设置由 Electron 主进程独占写入版本化 `settings.json`，使用原子写入和损坏回退；旧版渲染层 `localStorage` 仅做一次性迁移；显式终止保留已生成文件并归档部分结果。

在当前 macOS arm64 主机上的验证结果：

| 验证项 | 结果 | 说明 |
| --- | --- | --- |
| `python3 -m unittest discover -s tests` | 通过（191 个） | Python 后端、任务控制、暂停/终止、断点、历史、路径与配置回归全部通过 |
| `npm test` | 通过（35 个测试） | Electron 主进程、渲染层、设置、文件对话框和 IPC 回归全部通过 |
| `python3 -m py_compile`、`node --check`、`git diff --check` | 通过 | Python、Electron 主进程/预加载/渲染层语法及补丁检查通过 |
| `npx electron . --smoke-test` | 通过 | 开发模式主进程、后端和渲染器端到端启动通过 |
| 打包后端 `server_backend --smoke-playwright` | 通过 | Electron 启动路径注入 Electron Node；独立执行时可回退系统 Node，包内 Playwright driver + Chromium 实际启动通过 |
| `npm run build:mac` | 通过 | macOS arm64 目录版 `.app` 构建成功，后端已进入 `Contents/Resources` |
| 打包 `.app --smoke-test` | 通过 | `app.isPackaged` 分支从 `.app` 内后端路径启动并完成端到端探针 |

以下仍是发布前的平台实测 gate，不应被上述自动化结果替代：

- Windows 真机的 HWND 枚举、隐藏/恢复、任务栏、Alt+Tab、100%/125%/150% DPI 和多显示器行为；
- macOS 真机上以有界面模式启动随包 Chromium 后的辅助功能权限提示、窗口隐藏/恢复、Dock、Cmd+Tab、Mission Control 和多显示器行为；
- 真实登录/人工验证流程中专用浏览器身份、PID/窗口句柄追踪，以及不影响用户 Chrome 的验证；
- Windows 安装包构建、安装升级和快捷键冲突实测。当前主机已验证 macOS 目录版，不宣称 Windows 安装包已构建。

因此，代码和自动化 POC 已完成，可进入 Windows/macOS 平台联调与发布候选验证；平台 gate 通过前，不把“跨平台防偷窥”标记为最终发布承诺。

## 13. 已知限制

- 有界面 Chrome 兼容性更好，但不能保证网站绝对无法识别自动化行为；
- 暂停只能在安全检查点生效，不能保证在任意底层操作的中间瞬间冻结；
- 页面跳转、下载或网络请求可能导致暂停确认延迟；
- macOS 辅助功能权限和 Dock 的应用级显示行为需要单独适配；
- 系统 Chrome 的应用级图标不一定能通过隐藏单个窗口移除；
- 任务管理器、活动监视器、进程列表和录屏软件仍可能发现程序；
- 如果应用或系统崩溃，不能保证从任意浏览器操作中间位置精确恢复，需要通过任务检查点、作品对账和文件状态恢复；
- 全局快捷键只能报告注册成功或失败，不能保证给出具体冲突程序；
- 小窗在极小尺寸、高 DPI、超长文本或复杂角色配置下可能需要扩大窗口或切换完整模式。

## 14. 结论与第一阶段非目标

最终采用以下方案：

> 有界面专用浏览器 + 后台窗口隐藏/最小化 + Electron 窗口模式控制 + 后端权威任务状态机 + 协作式暂停/继续 + 明确终止语义 + 支持完整业务流程的小窗工作台 + 独立设置中心。

小窗模式必须具备完整业务能力，而不是只展示进度；暂停只暂停自动化任务，不关闭 Chrome；防偷窥模式隐藏小猪和专用浏览器；终止释放浏览器但保留已生成文件；所有窗口行为、任务控制和快捷键由对应控制器统一管理。

第一阶段暂不承诺：

- 在所有 Linux 桌面环境实现一致的窗口隐藏；
- 使用系统 Google Chrome 时保证 macOS Dock 完全无图标；
- 阻止进程列表、任务管理器或录屏软件发现程序；
- 从任意浏览器调用中间位置精确恢复；
- 在运行任务期间任意切换浏览器配置目录；
- 对系统快捷键冲突提供具体冲突程序名称。
