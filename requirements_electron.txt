# Electron 打包专用依赖（不含 gradio / pywebview）
# 用于 PyInstaller 打包 server.py → server_backend
# 安装: pip install -r requirements_electron.txt

# ---- 后端框架 ----
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.0.0

# ---- TTS 核心 ----
edge-tts>=7.2.0
aiohttp

# ---- 文档解析 ----
python-docx>=1.0.0
openpyxl>=3.1.0
lxml>=5.0.0
# 仅供 scripts/build_app_icons.py 构建图标；固定版本确保 CI 生成的 PNG/ICNS/ICO 可复现。
# PyInstaller 后端明确排除 PIL。
Pillow>=11.0.0

# ---- 音频处理 ----
pydub>=0.25.1
imageio-ffmpeg>=0.5.1

# ---- 文件上传 ----
# FastAPI 的 UploadFile/File 路由在运行时需要此包。
python-multipart

# ---- 打包工具 ----
pyinstaller>=6.0.0

# ---- TTSMaker 男声生成（打包内置）----
# Playwright: 浏览器自动化，操控 TTSMaker.cn 网站生成男声
# 固定到已验证版本；Chromium revision 必须与它的 browsers.json 精确匹配。
playwright==1.56.0
